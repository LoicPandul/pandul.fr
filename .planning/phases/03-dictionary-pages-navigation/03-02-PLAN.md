---
phase: 03-dictionary-pages-navigation
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - _layouts/definition.html
  - assets/css/style.css
  - assets/js/main.js
autonomous: true
requirements: [DICT-05, DICT-09, DICT-10]

must_haves:
  truths:
    - "Each definition page shows breadcrumbs: Dictionnaire > Category > Term"
    - "Each definition page shows a category badge linking to the category page"
    - "English term is displayed when present in front matter"
    - "Cross-references appear as clickable pills (not bullet list)"
    - "Prev/next navigation links appear within same category"
    - "Book CTA with Amazon + Bitcoin Bazar + GitHub buttons appears at bottom"
    - "Clicking an image opens a lightbox modal"
  artifacts:
    - path: "_layouts/definition.html"
      provides: "Enriched definition layout with breadcrumbs, badges, pills, nav, CTA, lightbox"
    - path: "assets/css/style.css"
      provides: "CSS for all definition page components"
    - path: "assets/js/main.js"
      provides: "Lightbox functionality"
  key_links:
    - from: "_layouts/definition.html"
      to: "/dictionnaire/categorie/{slug}/"
      via: "breadcrumb and badge links using page.category_slug"
      pattern: "category_slug"
    - from: "_layouts/definition.html"
      to: "/dictionnaire/{slug}/"
      via: "cross-ref pill links using ref.slug"
      pattern: "cross_references"
    - from: "assets/js/main.js"
      to: "_layouts/definition.html"
      via: "lightbox click handler on .definition-content img"
      pattern: "definition-content"
---

<objective>
Enrich the definition page layout with breadcrumbs, category badge, English term, cross-reference pills, prev/next navigation, book CTA, and image lightbox. Add all CSS for these components.

Purpose: Transforms the minimal definition layout into a rich, navigable page per D-10 through D-15 and UI-SPEC components 5-11.

Output: Enriched definition.html layout, extended style.css with dictionary component styles, lightbox JS in main.js.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dictionary-pages-navigation/03-CONTEXT.md
@.planning/phases/03-dictionary-pages-navigation/03-UI-SPEC.md
@.planning/phases/03-dictionary-pages-navigation/03-01-PLAN.md
@.claude/skills/frontend-design/SKILL.md
@.claude/skills/ui-ux-pro-max/SKILL.md

<interfaces>
<!-- Definition front matter after Plan 01 enrichment -->
```yaml
title: "ACINQ"
slug: "acinq"
permalink: /dictionnaire/acinq/
category: "ORGANISATION"
category_slug: "organisation"
letter: "A"
layout: definition
english_term: "ACINQ"
cross_references:
  - title: "LIGHTNING NETWORK"
    slug: "lightning-network"
  - title: "PHOENIX"
    slug: "phoenix"
prev_in_category:
  title: "Previous Term"
  slug: "previous-term"
next_in_category:
  title: "Next Term"
  slug: "next-term"
```

<!-- Existing CSS variables from style.css -->
```css
:root {
  --bg: #FFFFFF; --fg: #000000; --brand: #000000;
  --accent: #D04819; --accent-hover: #B83D14;
  --muted: #666666; --line: #E5E5E5;
  --wrap: 1100px; --prose: 680px; --radius: 6px;
  --space-xs: 4px; --space-sm: 8px; --space-md: 16px;
  --space-lg: 24px; --space-xl: 32px; --space-2xl: 48px; --space-3xl: 64px;
  --font-heading: "Outfit", system-ui, sans-serif;
  --font-body: "Inter", system-ui, sans-serif;
}
```

<!-- Existing button classes -->
.btn-primary: accent bg, white text, 44px min-height, 12px 24px padding
.btn-secondary: black bg, white text, same sizing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite definition layout with all enrichments</name>
  <files>_layouts/definition.html</files>
  <read_first>
    - _layouts/definition.html
    - _layouts/default.html
    - _layouts/category.html
    - .planning/phases/03-dictionary-pages-navigation/03-UI-SPEC.md
    - .planning/phases/03-dictionary-pages-navigation/03-CONTEXT.md
    - dictionnaire.md
  </read_first>
  <action>
Replace the current minimal definition.html with the full enriched layout. The layout inherits from `default`.

**Complete structure top to bottom:**

1. **Search bar (D-02)** — Search accessible on every definition page, placed above breadcrumbs:
```html
<div class="dict-search" role="search">
  <div class="dict-search-inner">
    <svg class="dict-search-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
    <input type="search" class="dict-search-input" placeholder="Rechercher une definition..." autocomplete="off" role="combobox" aria-expanded="false" aria-controls="searchResults" aria-activedescendant="">
    <ul id="searchResults" class="dict-search-results" role="listbox" aria-label="Resultats de recherche"></ul>
  </div>
</div>
```
The `dict-search-inner` wrapper constrains max-width to `var(--prose)` on definition pages.

2. **Breadcrumbs (D-10, UI-SPEC component 5):**
```html
<nav class="breadcrumbs" aria-label="Fil d'Ariane">
  <ol>
    <li><a href="/dictionnaire/">Dictionnaire</a></li>
    <li><a href="/dictionnaire/categorie/{{ page.category_slug }}/">{{ page.category }}</a></li>
    <li aria-current="page">{{ page.title }}</li>
  </ol>
</nav>
```

3. **H1 title** (the definition term)

4. **Meta line (D-11, UI-SPEC components 6 + 7):**
```html
<div class="definition-meta">
  <a href="/dictionnaire/categorie/{{ page.category_slug }}/" class="category-badge">{{ page.category }}</a>
  {% if page.english_term %}<span class="definition-english"><em>EN:</em> {{ page.english_term }}</span>{% endif %}
</div>
```

5. **Definition content** — `<div class="definition-content">{{ content }}</div>`

6. **Cross-references as pills (D-12, UI-SPEC component 8):**
```html
{% if page.cross_references.size > 0 %}
<section class="definition-see-also">
  <h2>Voir aussi</h2>
  <div class="cross-ref-pills">
    {% for ref in page.cross_references %}
    <a href="/dictionnaire/{{ ref.slug }}/" class="cross-ref-pill">{{ ref.title }}</a>
    {% endfor %}
  </div>
</section>
{% endif %}
```

7. **Prev/next navigation (D-13, UI-SPEC component 9):**
```html
<nav class="definition-nav" aria-label="Navigation dans la categorie">
  {% if page.prev_in_category %}
  <a href="/dictionnaire/{{ page.prev_in_category.slug }}/" class="def-nav-prev" aria-label="Definition precedente : {{ page.prev_in_category.title }}">
    &lsaquo; {{ page.prev_in_category.title }}
  </a>
  {% else %}
  <span></span>
  {% endif %}
  {% if page.next_in_category %}
  <a href="/dictionnaire/{{ page.next_in_category.slug }}/" class="def-nav-next" aria-label="Definition suivante : {{ page.next_in_category.title }}">
    {{ page.next_in_category.title }} &rsaquo;
  </a>
  {% else %}
  <span></span>
  {% endif %}
</nav>
```

8. **Book CTA (D-15, UI-SPEC component 11):**
```html
<aside class="definition-cta">
  <div class="definition-cta-book">
    <p class="definition-cta-heading">Ce dictionnaire existe en version imprimee</p>
    <div class="definition-cta-buttons">
      <a href="https://bitcoinbazar.fr/" class="btn btn-primary btn-sm" target="_blank" rel="noopener">Acheter — Bitcoin Bazar</a>
      <a href="https://www.amazon.fr/dp/B0GV1N6S1W" class="btn btn-primary btn-sm" target="_blank" rel="noopener">Acheter — Amazon</a>
    </div>
  </div>
  <div class="definition-cta-contrib">
    <p class="definition-cta-text">Une erreur ? Une amelioration ?</p>
    <div class="definition-cta-buttons">
      <a href="https://github.com/LoicPandul/Dictionnaire-de-Bitcoin" class="btn btn-secondary btn-sm" target="_blank" rel="noopener">Contribuer sur GitHub</a>
    </div>
  </div>
</aside>
```

Wrap the entire content in `<article class="definition-page">` and constrain the main content area width with the existing `.wrap` from `default.html`. The definition content should have `max-width: var(--prose)` for readability.
  </action>
  <verify>
    <automated>bundle exec jekyll build 2>&1 | tail -5 && grep -c "breadcrumbs" _site/dictionnaire/acinq/index.html && grep -c "cross-ref-pill" _site/dictionnaire/acinq/index.html && grep -c "definition-cta" _site/dictionnaire/acinq/index.html</automated>
  </verify>
  <acceptance_criteria>
    - _layouts/definition.html contains `class="breadcrumbs"`
    - _layouts/definition.html contains `aria-label="Fil d'Ariane"`
    - _layouts/definition.html contains `page.category_slug`
    - _layouts/definition.html contains `class="category-badge"`
    - _layouts/definition.html contains `class="definition-english"`
    - _layouts/definition.html contains `EN:`
    - _layouts/definition.html contains `class="cross-ref-pills"`
    - _layouts/definition.html contains `class="cross-ref-pill"`
    - _layouts/definition.html contains `class="definition-nav"`
    - _layouts/definition.html contains `prev_in_category`
    - _layouts/definition.html contains `next_in_category`
    - _layouts/definition.html contains `class="definition-cta"`
    - _layouts/definition.html contains `bitcoinbazar.fr`
    - _layouts/definition.html contains `amazon.fr/dp/B0GV1N6S1W`
    - _layouts/definition.html contains `github.com/LoicPandul/Dictionnaire-de-Bitcoin`
    - _layouts/definition.html contains `Contribuer sur GitHub`
    - _layouts/definition.html contains `dict-search`
    - _layouts/definition.html contains `role="combobox"`
    - `bundle exec jekyll build` completes without error
  </acceptance_criteria>
  <done>Definition layout shows breadcrumbs (Dictionnaire > Category > Term), category badge, English term, content, cross-ref pills, prev/next navigation, and book CTA on every definition page. Search bar present at top.</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for definition page components + lightbox JS</name>
  <files>assets/css/style.css, assets/js/main.js</files>
  <read_first>
    - assets/css/style.css
    - assets/js/main.js
    - _layouts/definition.html
    - .planning/phases/03-dictionary-pages-navigation/03-UI-SPEC.md
  </read_first>
  <action>
**IMPORTANT: Use the frontend-design and ui-ux-pro-max skills for a high-end, modern design. D-19 mandates exceptional design quality. Read `.claude/skills/frontend-design/SKILL.md` and `.claude/skills/ui-ux-pro-max/SKILL.md` before writing CSS.**

**CSS — Append to assets/css/style.css (do not modify existing rules):**

Add a new section `/* ============================================ Dictionnaire — Pages de definition ============================================ */`

Add `--surface: #F5F5F5;` to the existing `:root` block (new CSS variable per UI-SPEC).

**Breadcrumbs (UI-SPEC component 5):**
```css
.breadcrumbs { margin-bottom: var(--space-md); padding: 0; }
.breadcrumbs ol { list-style: none; padding: 0; margin: 0; display: flex; flex-wrap: wrap; align-items: center; gap: 0; }
.breadcrumbs li { font-family: var(--font-body); font-size: 14px; font-weight: 400; line-height: 1.4; color: var(--muted); }
.breadcrumbs li + li::before { content: " > "; margin: 0 6px; color: var(--muted); }
.breadcrumbs a { color: var(--muted); text-decoration: none; }
.breadcrumbs a:hover { text-decoration: underline; color: var(--fg); }
.breadcrumbs [aria-current="page"] { color: var(--fg); }
```

**Definition page container:**
```css
.definition-page { max-width: var(--prose); }
```

**Category badge (UI-SPEC component 6):**
```css
.category-badge {
  display: inline-flex; align-items: center;
  padding: 4px 12px; border-radius: 4px;
  background: var(--surface); font-size: 12px;
  font-weight: 700; font-family: var(--font-body);
  text-transform: uppercase; letter-spacing: 0.03em;
  color: var(--fg); text-decoration: none;
  transition: background 0.15s ease;
}
.category-badge:hover { background: var(--line); color: var(--fg); }
```

**Definition meta (badge + english term):**
```css
.definition-meta {
  display: flex; flex-wrap: wrap; align-items: center;
  gap: var(--space-sm); margin-bottom: var(--space-xl);
}
.definition-english {
  font-family: var(--font-body); font-size: 14px;
  font-style: italic; color: var(--muted); font-weight: 400;
}
.definition-english em { font-style: normal; }
```

**Definition content area:**
```css
.definition-content { line-height: 1.7; margin-bottom: var(--space-xl); }
.definition-content img { cursor: zoom-in; border-radius: var(--radius); margin: var(--space-md) 0; }
```

**Cross-reference pills (UI-SPEC component 8):**
```css
.definition-see-also { margin-bottom: var(--space-xl); }
.definition-see-also h2 { font-family: var(--font-heading); font-weight: 700; font-size: clamp(20px, 3vw, 22px); margin-bottom: var(--space-sm); }
.cross-ref-pills { display: flex; flex-wrap: wrap; gap: 8px; }
.cross-ref-pill {
  display: inline-flex; padding: 8px 12px; border-radius: 20px;
  background: var(--surface); color: var(--fg);
  font-size: 14px; font-weight: 400; font-family: var(--font-body);
  text-decoration: none; border: 1px solid var(--line);
  transition: all 0.15s ease;
}
.cross-ref-pill:hover { background: var(--fg); color: var(--bg); border-color: var(--fg); }
```

**Prev/next navigation (UI-SPEC component 9):**
```css
.definition-nav {
  display: flex; justify-content: space-between; align-items: center;
  padding: var(--space-lg) 0; border-top: 1px solid var(--line);
  margin-top: var(--space-xl);
}
.def-nav-prev, .def-nav-next {
  font-family: var(--font-body); font-size: 14px;
  color: var(--fg); text-decoration: none;
  transition: color 0.15s ease;
}
.def-nav-prev:hover, .def-nav-next:hover { color: var(--accent); }
```

**Book CTA (UI-SPEC component 11):**
```css
.definition-cta {
  padding: var(--space-lg); border: 1px solid var(--line);
  border-radius: var(--radius); background: var(--bg);
  margin-top: var(--space-xl);
}
.definition-cta-heading {
  font-family: var(--font-heading); font-weight: 700;
  font-size: clamp(16px, 1.05vw + 14px, 18px);
  color: var(--fg); margin: 0 0 var(--space-sm);
}
.definition-cta-buttons { display: flex; flex-wrap: wrap; gap: 8px; margin-top: var(--space-sm); }
.btn-sm { padding: 8px 16px; font-size: 14px; min-height: 36px; }
.definition-cta-contrib {
  border-top: 1px solid var(--line);
  margin-top: var(--space-md); padding-top: var(--space-md);
}
.definition-cta-text { font-family: var(--font-body); font-size: 14px; color: var(--muted); margin: 0 0 var(--space-sm); }
```

**Small button on mobile:**
```css
@media (max-width: 719px) {
  .definition-cta-buttons { flex-direction: column; }
  .definition-cta-buttons .btn { width: 100%; }
  .definition-nav { flex-direction: column; gap: var(--space-sm); align-items: flex-start; }
}
```

**Lightbox (UI-SPEC component 10):**
```css
.lightbox-overlay {
  position: fixed; inset: 0;
  background: rgba(0,0,0,0.85);
  z-index: 2000; display: flex;
  align-items: center; justify-content: center;
  opacity: 0; transition: opacity 0.2s ease;
}
.lightbox-overlay.active { opacity: 1; }
.lightbox-overlay img {
  max-width: 90vw; max-height: 85vh;
  object-fit: contain; border-radius: var(--radius);
  cursor: default;
}
.lightbox-close {
  position: absolute; top: 16px; right: 16px;
  width: 40px; height: 40px;
  background: none; border: none;
  color: #fff; font-size: 28px;
  cursor: pointer; display: flex;
  align-items: center; justify-content: center;
}
@media (max-width: 719px) {
  .lightbox-overlay img { max-width: 95vw; }
}
```

**JS — Append lightbox functionality to assets/js/main.js:**

Add inside the existing `DOMContentLoaded` handler (before the closing `});`):

```javascript
// Lightbox — zoom sur les images des definitions
const defContent = document.querySelector('.definition-content');
if (defContent) {
  defContent.addEventListener('click', (e) => {
    if (e.target.tagName !== 'IMG') return;

    const overlay = document.createElement('div');
    overlay.className = 'lightbox-overlay';
    overlay.setAttribute('role', 'dialog');
    overlay.setAttribute('aria-modal', 'true');
    overlay.innerHTML = '<button class="lightbox-close" aria-label="Fermer">&times;</button><img src="' + e.target.src + '" alt="' + (e.target.alt || '') + '">';

    const previousFocus = document.activeElement;
    document.body.appendChild(overlay);

    // Fade in
    requestAnimationFrame(() => { overlay.classList.add('active'); });
    overlay.querySelector('.lightbox-close').focus();

    function closeLightbox() {
      overlay.classList.remove('active');
      setTimeout(() => {
        overlay.remove();
        if (previousFocus) previousFocus.focus();
      }, 150);
    }

    overlay.addEventListener('click', (ev) => {
      if (ev.target === overlay || ev.target.classList.contains('lightbox-close')) closeLightbox();
    });
    document.addEventListener('keydown', function handler(ev) {
      if (ev.key === 'Escape') {
        closeLightbox();
        document.removeEventListener('keydown', handler);
      }
    });
  });
}
```

Use `textContent` and attribute assignment (not innerHTML for user-facing data) to prevent XSS. The alt text in the lightbox image is from trusted markdown content (generated by sync script), so string interpolation is safe here.
  </action>
  <verify>
    <automated>bundle exec jekyll build 2>&1 | tail -3 && grep -c "lightbox-overlay" assets/css/style.css && grep -c "cross-ref-pill" assets/css/style.css && grep -c "lightbox" assets/js/main.js</automated>
  </verify>
  <acceptance_criteria>
    - assets/css/style.css contains `--surface: #F5F5F5`
    - assets/css/style.css contains `.breadcrumbs`
    - assets/css/style.css contains `.category-badge`
    - assets/css/style.css contains `.cross-ref-pill`
    - assets/css/style.css contains `.cross-ref-pill:hover`
    - assets/css/style.css contains `background: var(--fg)` (pill hover inversion)
    - assets/css/style.css contains `.definition-nav`
    - assets/css/style.css contains `.definition-cta`
    - assets/css/style.css contains `.btn-sm`
    - assets/css/style.css contains `.lightbox-overlay`
    - assets/css/style.css contains `.lightbox-close`
    - assets/css/style.css contains `z-index: 2000`
    - assets/js/main.js contains `lightbox-overlay`
    - assets/js/main.js contains `definition-content`
    - assets/js/main.js contains `closeLightbox`
    - assets/js/main.js contains `aria-modal`
    - `bundle exec jekyll build` completes without error
  </acceptance_criteria>
  <done>All CSS for definition page components (breadcrumbs, badge, pills, nav, CTA, lightbox) added. Lightbox JS opens images in modal with fade, focus trap, Escape/click-to-close. High-end design per D-19 with smooth transitions and consistent spacing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Definition content display | Pre-rendered markdown from trusted repo, no user input |
| Image lightbox src | Uses img.src from page content (trusted), not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | Lightbox image src | accept | Image sources come from trusted sync script output, no user input vector |
| T-03-04 | Tampering | Cross-ref pill links | accept | Links are hardcoded to /dictionnaire/{slug}/ from trusted front matter |
</threat_model>

<verification>
1. `bundle exec jekyll build` succeeds
2. Definition pages show breadcrumbs, badge, English term, pills, nav, CTA
3. Lightbox opens when clicking images in definitions
4. CSS variables used consistently (no hardcoded colors)
5. Mobile responsive: CTA buttons stack, nav stacks
</verification>

<success_criteria>
- Definition layout enriched with all 7 sections (search, breadcrumbs, meta, content, see-also, nav, CTA)
- CSS added for all components following UI-SPEC exactly
- Lightbox JS functional with keyboard support (Escape to close)
- All buttons use existing btn-primary/btn-secondary classes with btn-sm variant
- Mobile responsive per UI-SPEC breakpoints
</success_criteria>

<output>
After completion, create `.planning/phases/03-dictionary-pages-navigation/03-02-SUMMARY.md`
</output>
