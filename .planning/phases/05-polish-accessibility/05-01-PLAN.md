---
phase: 05-polish-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _layouts/default.html
  - _layouts/definition.html
  - _layouts/category.html
  - _layouts/letter.html
  - dictionnaire.md
  - _dictionnaire/adaptor-signature.md
  - _dictionnaire/adresse-statique.md
  - _dictionnaire/ajustement-de-la-difficulte.md
  - _dictionnaire/bip-0020.md
  - _dictionnaire/bip-0158.md
  - _dictionnaire/binohash.md
  - _dictionnaire/bulletproofs.md
  - _dictionnaire/channel-jamming.md
  - _dictionnaire/cle-de-revocation.md
  - _dictionnaire/cle-publique-compressee.md
  - _dictionnaire/coinswap.md
  - _dictionnaire/commitment.md
  - _dictionnaire/concatenation.md
  - _dictionnaire/courbe-elliptique.md
  - _dictionnaire/derivation-endurcie.md
  - _dictionnaire/derivation-normale.md
  - _dictionnaire/dlp-discreet-log-probleme.md
  - _dictionnaire/ecdsa.md
  - _dictionnaire/embranchement-naturel.md
  - _dictionnaire/entropie-analyse.md
  - _dictionnaire/fonction-de-hachage.md
  - _dictionnaire/gcs-golomb-coded-set.md
  - _dictionnaire/htlc.md
  - _dictionnaire/label-silent-payments.md
  - _dictionnaire/logarithme-discret.md
  - _dictionnaire/millisatoshi.md
  - _dictionnaire/nbits.md
  - _dictionnaire/oboe-off-by-one-error.md
  - _dictionnaire/octet.md
  - _dictionnaire/p2tr.md
  - _dictionnaire/pay-to-contract.md
  - _dictionnaire/prefixes-binaires.md
  - _dictionnaire/preimage.md
  - _dictionnaire/schnorr-protocole.md
  - _dictionnaire/secp256k1.md
  - _dictionnaire/secp256r1.md
  - _dictionnaire/sign-to-contract.md
  - _dictionnaire/silent-payments.md
  - _dictionnaire/spark-tree.md
  - _dictionnaire/taproot.md
  - _dictionnaire/time-warp.md
  - _dictionnaire/tweak.md
  - _dictionnaire/weight-unit.md
  - _dictionnaire/x-only-public-keys.md
  - _dictionnaire/xor.md
  - assets/fonts/archive/
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "KaTeX CSS and JS are NOT loaded on pages without math: true"
    - "KaTeX CSS and JS ARE loaded on the 45 definitions with math: true"
    - "Skip-to-content link is first focusable element in the DOM"
    - "Skip-to-content link targets #main-content on the main element"
    - "Search inputs have aria-label on all dictionary layouts"
    - "Archived fonts no longer exist on disk"
  artifacts:
    - path: "_layouts/default.html"
      provides: "Conditional KaTeX, skip-to-content, main id"
      contains: "if page.math"
    - path: "_layouts/definition.html"
      provides: "aria-label on search input"
      contains: "aria-label"
    - path: "_layouts/category.html"
      provides: "aria-label on search input"
      contains: "aria-label"
    - path: "_layouts/letter.html"
      provides: "aria-label on search input"
      contains: "aria-label"
  key_links:
    - from: "_layouts/default.html"
      to: "45 _dictionnaire/ files"
      via: "page.math front matter flag"
      pattern: "math: true"
    - from: "skip-to-content <a>"
      to: "<main id='main-content'>"
      via: "href='#main-content'"
      pattern: "id=\"main-content\""
---

<objective>
Optimize page load performance by making KaTeX conditional, add WCAG accessibility markup (skip-to-content, aria-labels), and delete unused archived fonts.

Purpose: Remove 3 CDN requests from ~1363 non-math pages, make the site keyboard-navigable with skip links, and clean ~1MB of dead font weight from the repo.
Output: Modified default.html with conditional KaTeX + skip-to-content + main id, 45 definition files with math: true, 4 layouts with aria-label on search, deleted archive fonts directory.
</objective>

<execution_context>
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-accessibility/05-CONTEXT.md
@.planning/phases/05-polish-accessibility/05-RESEARCH.md
@.planning/phases/05-polish-accessibility/05-UI-SPEC.md

<interfaces>
From _layouts/default.html (current KaTeX block, lines 14-17):
```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.css" crossorigin="anonymous">
<script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.js" crossorigin="anonymous"></script>
<script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/contrib/auto-render.min.js" crossorigin="anonymous"
  onload="renderMathInElement(document.body, {delimiters: [{left: '$$', right: '$$', display: true}, {left: '\\[', right: '\\]', display: true}, {left: '\\(', right: '\\)', display: false}, {left: '$', right: '$', display: false}]});"></script>
```

From _layouts/default.html (current body opening, lines 19-48):
```html
<body>
  <header class="site-header">
    ...
  </header>
  <main class="site-main">
    <div class="wrap">
      {{ content }}
    </div>
  </main>
```

Note: `<main>` currently has NO id attribute and NO skip-to-content link exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conditional KaTeX, skip-to-content link, aria-labels, and math flags</name>
  <files>
    _layouts/default.html,
    _layouts/definition.html,
    _layouts/category.html,
    _layouts/letter.html,
    dictionnaire.md
  </files>
  <read_first>
    _layouts/default.html,
    _layouts/definition.html,
    _layouts/category.html,
    _layouts/letter.html,
    dictionnaire.md,
    .planning/phases/05-polish-accessibility/05-UI-SPEC.md
  </read_first>
  <action>
**In _layouts/default.html:**

1. Wrap the KaTeX block (lines 14-17) in a Liquid conditional. Replace the 3 unconditional KaTeX lines with:
```liquid
{%- comment -%} KaTeX — chargement conditionnel {%- endcomment -%}
{% if page.math %}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.css" crossorigin="anonymous">
<script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.js" crossorigin="anonymous"></script>
<script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/contrib/auto-render.min.js" crossorigin="anonymous"
  onload="renderMathInElement(document.body, {delimiters: [{left: '$$', right: '$$', display: true}, {left: '\\[', right: '\\]', display: true}, {left: '\\(', right: '\\)', display: false}, {left: '$', right: '$', display: false}]});"></script>
{% endif %}
```

2. Add skip-to-content link as first element inside `<body>`, BEFORE `<header>`:
```html
<a href="#main-content" class="skip-to-content">Aller au contenu principal</a>
```

3. Add `id="main-content"` to the `<main>` element:
```html
<main class="site-main" id="main-content">
```

**In _layouts/definition.html, _layouts/category.html, _layouts/letter.html, and dictionnaire.md:**

Find the search `<input>` element (it has `type="search"` or `role="combobox"` or `id="dictSearch"`) and add the attribute `aria-label="Rechercher une definition"` to it. Do NOT add aria-label if one already exists. The exact attribute to add: `aria-label="Rechercher une definition"`.

**In 45 _dictionnaire/ files (per D-01):**

Add `math: true` to the YAML front matter of each of these 45 definition files. Insert it as a new line in the front matter block (between the `---` delimiters), after the last existing field. The complete list of slugs:
adaptor-signature, adresse-statique, ajustement-de-la-difficulte, bip-0020, bip-0158, binohash, bulletproofs, channel-jamming, cle-de-revocation, cle-publique-compressee, coinswap, commitment, concatenation, courbe-elliptique, derivation-endurcie, derivation-normale, dlp-discreet-log-probleme, ecdsa, embranchement-naturel, entropie-analyse, fonction-de-hachage, gcs-golomb-coded-set, htlc, label-silent-payments, logarithme-discret, millisatoshi, nbits, oboe-off-by-one-error, octet, p2tr, pay-to-contract, prefixes-binaires, preimage, schnorr-protocole, secp256k1, secp256r1, sign-to-contract, silent-payments, spark-tree, taproot, time-warp, tweak, weight-unit, x-only-public-keys, xor

For each file, read it first, then add `math: true` as the last line before the closing `---` of the front matter.
  </action>
  <verify>
    <automated>grep -c "if page.math" _layouts/default.html && grep -c "skip-to-content" _layouts/default.html && grep -c "id=\"main-content\"" _layouts/default.html && grep -c "aria-label" _layouts/definition.html && grep -c "aria-label=\"Rechercher une definition\"" _layouts/category.html && grep -c "aria-label=\"Rechercher une definition\"" _layouts/letter.html && grep -l "math: true" _dictionnaire/ecdsa.md _dictionnaire/taproot.md _dictionnaire/secp256k1.md</automated>
  </verify>
  <acceptance_criteria>
    - _layouts/default.html contains the string `{% if page.math %}` before the KaTeX link tag
    - _layouts/default.html contains the string `{% endif %}` after the auto-render script tag
    - _layouts/default.html contains `<a href="#main-content" class="skip-to-content">Aller au contenu principal</a>` as first child of body, before header
    - _layouts/default.html contains `id="main-content"` on the main element
    - _layouts/definition.html contains `aria-label="Rechercher une definition"` on the search input
    - _layouts/category.html contains `aria-label="Rechercher une definition"` on the search input
    - _layouts/letter.html contains `aria-label="Rechercher une definition"` on the search input
    - dictionnaire.md contains `aria-label="Rechercher une definition"` on the search input
    - All 45 _dictionnaire/ files listed above contain `math: true` in their front matter
    - `grep -rl "math: true" _dictionnaire/ | wc -l` outputs 45
  </acceptance_criteria>
  <done>KaTeX loads only on math pages, skip-to-content link exists and targets main content, all search inputs have aria-label, 45 definitions have math: true flag</done>
</task>

<task type="auto">
  <name>Task 2: Delete archived fonts directory</name>
  <files>assets/fonts/archive/</files>
  <read_first>assets/css/style.css</read_first>
  <action>
Delete the entire `assets/fonts/archive/` directory and all its contents (per D-02 and D-10). The directory contains 5 unused font files:
- JetBrainsMono-Variable.ttf
- Lora-Italic-Variable.ttf
- Lora-Variable.ttf
- Manrope-Variable.ttf
- Montserrat-Variable.ttf

Before deleting, verify that style.css does NOT reference any of these fonts (it uses Outfit and Inter only). Then run:
```bash
rm -rf assets/fonts/archive/
```

Confirm deletion with `ls assets/fonts/` which should show only: Inter-Variable.ttf, Outfit-Variable.ttf (and no archive/ subdirectory).
  </action>
  <verify>
    <automated>test ! -d assets/fonts/archive && echo "PASS: archive deleted" && ls assets/fonts/</automated>
  </verify>
  <acceptance_criteria>
    - Directory `assets/fonts/archive/` does not exist
    - `ls assets/fonts/` outputs only Inter-Variable.ttf and Outfit-Variable.ttf
    - No references to Lora, Montserrat, Manrope, or JetBrainsMono in assets/css/style.css
  </acceptance_criteria>
  <done>Archived fonts directory removed, ~1MB reclaimed, only active fonts remain in assets/fonts/</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. This plan modifies HTML templates, front matter, and deletes static files. No user input processing, no authentication, no data flow changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | KaTeX conditional | accept | KaTeX is a public CDN resource; conditional loading is a performance optimization, not a security boundary |
</threat_model>

<verification>
- `grep "if page.math" _layouts/default.html` returns a match
- `grep "skip-to-content" _layouts/default.html` returns a match
- `grep -rl "math: true" _dictionnaire/ | wc -l` returns 45
- `test ! -d assets/fonts/archive && echo PASS` returns PASS
- `bundle exec jekyll build 2>&1 | tail -5` completes without errors
</verification>

<success_criteria>
- KaTeX conditionally loaded: 3 CDN requests removed from ~1363 non-math pages
- Skip-to-content link present and functional in DOM
- All 4 dictionary search inputs have aria-label
- 45 math definitions have math: true flag
- Archive fonts deleted (~1MB saved)
- Jekyll build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-accessibility/05-01-SUMMARY.md`
</output>
