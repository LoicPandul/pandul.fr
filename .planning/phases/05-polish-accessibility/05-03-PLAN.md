---
phase: 05-polish-accessibility
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - .gitignore
autonomous: false
requirements:
  - PERF-03

must_haves:
  truths:
    - "No .planning/ files appear in git ls-files"
    - "CLAUDE.md does not appear in git ls-files"
    - ".gitignore covers all required patterns"
    - "Planning files still exist on local disk"
    - "Contact info (email + Lightning) is visible in footer"
  artifacts:
    - path: ".gitignore"
      provides: "Complete coverage of all dev/build artifacts"
      contains: ".planning/"
  key_links:
    - from: ".gitignore"
      to: "git index"
      via: "git rm --cached removes from tracking"
      pattern: "git ls-files .planning/"
---

<objective>
Remove planning artifacts from the public git repository index and verify complete .gitignore coverage, then visually verify the full phase deliverables.

Purpose: Ensure the public GitHub repo does not expose internal planning files (28 files in .planning/ and CLAUDE.md), and confirm that all Phase 5 changes work correctly together.
Output: Clean git index with no planning files tracked, verified .gitignore, visual confirmation of accessibility and performance improvements.
</objective>

<execution_context>
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-accessibility/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove planning files from git index and verify .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    .gitignore
  </read_first>
  <action>
**Step 1: Verify .gitignore coverage (per D-11)**

Read `.gitignore` and verify it contains ALL of these entries:
- `.planning/`
- `.claude/`
- `CLAUDE.md`
- `_site/`
- `.jekyll-cache/`
- `.sass-cache/`
- `.bundle/`

The current .gitignore already has all 7 entries. If any are missing, add them.

**Step 2: Remove planning files from git index (per D-08)**

Run these commands to untrack planning files WITHOUT deleting them locally:
```bash
git rm --cached -r .planning/
git rm --cached CLAUDE.md
```

**CRITICAL: Use `--cached` flag. Without it, files get deleted from disk.**

**Step 3: Check _site/ tracking (per D-09)**

Run: `git ls-files _site/ | wc -l`
If the output is 0, no action needed (_site/ is not tracked).
If the output is > 0, run: `git rm --cached -r _site/`

**Step 4: Verify results**

Run: `git ls-files .planning/ CLAUDE.md | wc -l` — must output 0.
Run: `ls .planning/STATE.md` — must succeed (file still exists on disk).
Run: `git status` — should show .planning/ files and CLAUDE.md as "deleted" in staging.

**Step 5: Commit the cleanup**

Stage .gitignore if modified, and commit all changes:
```bash
git add .gitignore
git commit -m "chore(05): remove planning artifacts from git index

Remove .planning/ (28 files) and CLAUDE.md from git tracking.
Files remain on local disk, covered by .gitignore.

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>git ls-files .planning/ CLAUDE.md 2>/dev/null | wc -l && ls .planning/STATE.md > /dev/null 2>&1 && echo "LOCAL_EXISTS" && git ls-files _site/ 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files .planning/ | wc -l` outputs 0
    - `git ls-files CLAUDE.md | wc -l` outputs 0
    - `git ls-files _site/ | wc -l` outputs 0
    - `ls .planning/STATE.md` succeeds (file exists locally)
    - `ls CLAUDE.md` succeeds (file exists locally)
    - `.gitignore` contains all 7 patterns: .planning/, .claude/, CLAUDE.md, _site/, .jekyll-cache/, .sass-cache/, .bundle/
    - A git commit exists with message containing "remove planning artifacts"
  </acceptance_criteria>
  <done>Planning files removed from git index, still exist locally, .gitignore verified complete, commit created</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of Phase 5</name>
  <files>none</files>
  <action>
Human verification checkpoint. Present the verification steps below to the user and wait for approval. No files are modified in this task.
  </action>
  <what-built>
Phase 5 deliverables:
- KaTeX conditional loading (only on 45 math definition pages)
- Skip-to-content link (visible on Tab from page load)
- aria-label on all dictionary search inputs
- Focus states on all dictionary interactive elements
- CSS media queries consolidated (10 blocks merged to 1 for 719px)
- Archived fonts deleted (~1MB savings)
- Planning files removed from public git index
- Contact info already in footer (email + Lightning)
  </what-built>
  <how-to-verify>
1. Run `bundle exec jekyll serve` and open http://localhost:4000
2. Press Tab immediately after page load — verify skip-to-content link appears ("Aller au contenu principal") with black background
3. Press Enter on skip link — verify focus jumps to main content area
4. Navigate to /dictionnaire/ — Tab through A-Z letters, verify visible orange focus ring
5. Tab to search input — verify it has focus ring; check source for aria-label
6. Navigate to a math definition (e.g., /dictionnaire/ecdsa/) — verify math formulas render correctly
7. Navigate to a non-math definition (e.g., /dictionnaire/bitcoin/) — view source, confirm NO KaTeX script tags in head
8. Check footer on any page — confirm info@pandul.fr and sats@pandul.fr are visible
9. Run `git ls-files .planning/ CLAUDE.md | wc -l` — confirm 0 (not tracked)
  </how-to-verify>
  <verify>
    <automated>bundle exec jekyll build 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms all Phase 5 changes work: skip-to-content, focus states, conditional KaTeX, contact visible, repo clean</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public repo | Planning files contain internal project details that should not be public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | .planning/ in git | mitigate | `git rm --cached -r .planning/` removes from index; .gitignore prevents re-addition; verify with `git ls-files` |
| T-05-04 | Information Disclosure | CLAUDE.md in git | mitigate | `git rm --cached CLAUDE.md` removes from index; .gitignore covers CLAUDE.md |
</threat_model>

<verification>
- `git ls-files .planning/ CLAUDE.md | wc -l` returns 0
- `.planning/STATE.md` exists on local filesystem
- `.gitignore` contains all 7 required patterns
- Human confirms: skip-to-content works, focus states visible, math renders, contact visible
</verification>

<success_criteria>
- Planning files untracked from git but preserved locally
- .gitignore covers all build/dev artifacts
- Human confirms all Phase 5 accessibility and performance changes work correctly
- PERF-03 confirmed satisfied (contact info visible in footer)
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-accessibility/05-03-SUMMARY.md`
</output>
