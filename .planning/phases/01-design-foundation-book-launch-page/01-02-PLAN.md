---
phase: 01-design-foundation-book-launch-page
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - dictionnaire.md
  - index.md
  - apropos.md
  - _config.yml
autonomous: false
requirements: [UI-04, BOOK-01, BOOK-02, BOOK-03]

must_haves:
  truths:
    - "Dictionary page displays book cover image prominently above the fold"
    - "Dictionary page has two Amazon purchase links with correct ASINs (broche B0GV1N6S1W, relie B0GV1X52YD)"
    - "Dictionary page has an Acheter sur Bitcoin Bazar link"
    - "Blog files and post directory are fully deleted from the repository"
    - "No inline styles remain in any content page"
    - "All content pages render correctly with the new design system from Plan 01"
  artifacts:
    - path: "dictionnaire.md"
      provides: "Book promo section with cover image and purchase links"
      contains: "book-promo"
    - path: "index.md"
      provides: "Homepage adapted to new design (no inline styles)"
    - path: "apropos.md"
      provides: "About page adapted to new design (no inline styles)"
    - path: "_config.yml"
      provides: "Jekyll config without jekyll-feed plugin"
  key_links:
    - from: "dictionnaire.md"
      to: "assets/img/cover-dictionnaire.png"
      via: "img src with relative_url Liquid filter"
      pattern: "cover-dictionnaire"
    - from: "dictionnaire.md"
      to: "https://www.amazon.fr/dp/B0GV1N6S1W"
      via: "anchor href"
      pattern: "B0GV1N6S1W"
    - from: "dictionnaire.md"
      to: "https://www.amazon.fr/dp/B0GV1X52YD"
      via: "anchor href"
      pattern: "B0GV1X52YD"
    - from: "dictionnaire.md"
      to: "https://bitcoinbazar.fr/"
      via: "anchor href"
      pattern: "bitcoinbazar.fr"
---

<objective>
Finalize content pages for the new design system, ensure book promo section is complete on the dictionary page, fully remove blog remnants, and visually verify the complete site.

Purpose: With the design system rebuilt in Plan 01, content pages need to work correctly with the new typography and layout. The book launch page is time-critical (~1 week). Blog deletion must be complete. A human checkpoint verifies the visual result across viewports.

Output: Clean content pages, complete book promo, blog fully removed, human-verified visual result.
</objective>

<execution_context>
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-foundation-book-launch-page/01-CONTEXT.md
@.planning/phases/01-design-foundation-book-launch-page/01-RESEARCH.md
@.planning/phases/01-design-foundation-book-launch-page/01-UI-SPEC.md
@.planning/phases/01-design-foundation-book-launch-page/01-01-SUMMARY.md

<interfaces>
<!-- Book promo HTML structure (from dictionnaire.md -- already implemented) -->
<section class="book-promo">
  <div class="book-promo-inner">
    <div class="book-cover">
      <img src="{{ '/assets/img/cover-dictionnaire.png' | relative_url }}"
           alt="Couverture -- Dictionnaire de Bitcoin"
           loading="eager" />
    </div>
    <div class="book-info">
      <h2>Dictionnaire de Bitcoin -- Le Livre</h2>
      <p>1408 definitions du vocabulaire technique de Bitcoin. Disponible en version imprimee.</p>
      <div class="book-links">
        <a href="https://www.amazon.fr/dp/B0GV1N6S1W" class="btn btn-primary" target="_blank" rel="noopener">Amazon -- Broche</a>
        <a href="https://www.amazon.fr/dp/B0GV1X52YD" class="btn btn-primary" target="_blank" rel="noopener">Amazon -- Relie</a>
        <a href="https://bitcoinbazar.fr/" class="btn btn-secondary" target="_blank" rel="noopener">Acheter sur Bitcoin Bazar</a>
      </div>
    </div>
  </div>
</section>

<!-- CSS classes from Plan 01 that content pages consume -->
.book-promo, .book-promo-inner, .book-cover, .book-info, .book-links
.btn-primary, .btn-secondary
.hero
.avatar, .avatar--center
.pbn-thumb
.tiny
.wrap, .site-main
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content pages cleanup + book promo verification + blog deletion</name>
  <files>dictionnaire.md, index.md, apropos.md, _config.yml</files>
  <read_first>
    - dictionnaire.md (current state -- verify book promo section is complete and correct)
    - index.md (current state -- check for inline styles, adapt to new design)
    - apropos.md (current state -- check for inline styles)
    - _config.yml (verify jekyll-feed removed)
    - assets/css/style.css (from Plan 01 -- check what CSS classes are available)
    - .planning/phases/01-design-foundation-book-launch-page/01-UI-SPEC.md (copywriting contract, Amazon URLs)
  </read_first>
  <action>
**1. Verify dictionnaire.md book promo section:**

The file should already contain the book promo section. Verify it matches the UI-SPEC copywriting contract exactly:
- Title: "Dictionnaire de Bitcoin -- Le Livre"
- Description: "1408 definitions du vocabulaire technique de Bitcoin. Disponible en version imprimee."
- Button 1: `href="https://www.amazon.fr/dp/B0GV1N6S1W"` with class `btn btn-primary`, text "Amazon -- Broche"
- Button 2: `href="https://www.amazon.fr/dp/B0GV1X52YD"` with class `btn btn-primary`, text "Amazon -- Relie"
- Button 3: `href="https://bitcoinbazar.fr/"` with class `btn btn-secondary`, text "Acheter sur Bitcoin Bazar"
- All links: `target="_blank" rel="noopener"`
- Cover image: `src="{{ '/assets/img/cover-dictionnaire.png' | relative_url }}"` with `loading="eager"` and `alt="Couverture -- Dictionnaire de Bitcoin"`
- Page front matter: `title: "Dictionnaire"`, `permalink: /dictionnaire/`

If any element is missing or incorrect, fix it. If the section is complete and correct, leave it as-is.

**2. Clean index.md:**

Verify no inline `style=""` attributes or `<style>` blocks remain. All styling should use CSS classes from style.css:
- Thumbnails should use `class="pbn-thumb"` on the link wrapper
- Images should use proper `alt`, `loading="lazy"`, and `aria-label` on links
- No `style=""` attributes on any element

**3. Clean apropos.md:**

Verify no inline `style=""` attributes. The avatar image should use:
```html
<img src="{{ '/assets/img/profile.png' | relative_url }}"
     alt="Portrait de Loic Morel"
     class="avatar avatar--center"
     width="180" height="180" loading="lazy" decoding="async">
```
No `<p style="text-align:center">` wrapper -- centering is via .avatar--center CSS class.

**4. Verify _config.yml:**

Ensure `jekyll-feed` is NOT in the plugins list. Only these plugins should remain:
- jekyll-seo-tag
- jekyll-sitemap
- jekyll-redirect-from

Ensure no `exclude: [post/]` line (post/ should just be deleted, not excluded).

**5. Delete blog remnants (UI-04):**

Delete `blog.md` if it still exists: `rm -f blog.md`
Delete the `post/` directory completely: `rm -rf post/`

These are the last blog remnants. The `post/` directory currently has 6 empty subdirectories (no files) -- a Git artifact that should be cleaned up.
  </action>
  <verify>
    <automated>grep "B0GV1N6S1W" dictionnaire.md && grep "B0GV1X52YD" dictionnaire.md && grep "bitcoinbazar.fr" dictionnaire.md && grep "cover-dictionnaire" dictionnaire.md && grep -c "style=" index.md | grep "^0$" && grep -c "style=" apropos.md | grep "^0$" && grep -c "jekyll-feed" _config.yml | grep "^0$" && test ! -f blog.md && test ! -d post && echo "ALL PASS"</automated>
  </verify>
  <acceptance_criteria>
    - dictionnaire.md contains `B0GV1N6S1W` (Amazon broche ASIN)
    - dictionnaire.md contains `B0GV1X52YD` (Amazon relie ASIN)
    - dictionnaire.md contains `bitcoinbazar.fr` (Bitcoin Bazar link)
    - dictionnaire.md contains `cover-dictionnaire` (book cover image reference)
    - dictionnaire.md contains `book-promo` (section class)
    - dictionnaire.md contains `btn-primary` (button class on Amazon links)
    - dictionnaire.md contains `btn-secondary` (button class on Bitcoin Bazar link)
    - dictionnaire.md contains `target="_blank"` and `rel="noopener"` on all purchase links
    - index.md contains 0 occurrences of `style=`
    - apropos.md contains 0 occurrences of `style=`
    - apropos.md contains `avatar--center` (CSS class for centering)
    - _config.yml does NOT contain `jekyll-feed`
    - blog.md does NOT exist on disk
    - post/ directory does NOT exist on disk
    - `jekyll build` succeeds with exit code 0
  </acceptance_criteria>
  <done>All content pages clean and compatible with new design system. Book promo section verified with all purchase links and cover image. Blog fully deleted (blog.md and post/ directory). Jekyll build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete redesign</name>
  <files>none (verification only)</files>
  <action>
Start `jekyll serve --livereload` and present the site for human visual verification. The human will check typography, header, footer, buttons, book promo, blog deletion, responsive layout, and about page rendering across desktop (1440px) and mobile (375px) viewports. Run the server and provide the URL http://localhost:4000 to the user.
  </action>
  <what-built>
Complete visual redesign of pandul.fr:
- New font pair (geometric headings + readable body) replacing JetBrains Mono + Manrope
- Redesigned header with rethought element disposition
- Redesigned footer with fixed mobile bug (no gray nav backgrounds)
- New button styles with readable hover states
- Book promo section on /dictionnaire/ with cover image and purchase links
- Blog fully deleted
- Mobile-first responsive at 375px, 720px, 980px+
  </what-built>
  <how-to-verify>
Run `jekyll serve --livereload` and test all of the following:

**1. Typography (D-05/D-06/D-07/D-08)**
- Open http://localhost:4000 at 1440px width
- Headings should use a geometric/modern font (NOT JetBrains Mono)
- Body text should use a classic readable font (NOT Manrope)
- Both fonts should feel modern, coherent, and distinctive

**2. Header (D-12)**
- Header card should feel redesigned -- not just the old layout with new colors
- Verify: logo, "Loic Morel", Accueil, Dictionnaire, A propos, X icon, GitHub icon all visible
- Scroll down past 120px: header should hide. Scroll up: header should reappear.

**3. Footer (D-16, D-17)**
- Resize to 375px (mobile): footer navigation links should have NO gray background
- Desktop: footer should show brand + nav + contact info in columns

**4. Buttons (D-14, D-15)**
- Navigate to http://localhost:4000/dictionnaire/
- Hover over "Amazon -- Broche" button: text should remain clearly readable (white on darker orange)
- Hover over "Acheter sur Bitcoin Bazar": should show clear visual change

**5. Book Promo (BOOK-01/02/03)**
- At 1440px: cover image on left, book info on right, three buttons visible
- At 375px: cover image centered on top, info below, buttons stacked
- Click "Amazon -- Broche": opens amazon.fr/dp/B0GV1N6S1W in new tab
- Click "Amazon -- Relie": opens amazon.fr/dp/B0GV1X52YD in new tab

**6. Blog Deletion (UI-04)**
- Navigate to http://localhost:4000/blog/ -- should show 404 page

**7. Responsive (UI-02)**
- Check all pages at 375px, 720px, and 1440px
- No horizontal scroll at any width
- Text readable at all sizes

**8. About Page**
- Navigate to http://localhost:4000/a-propos/
- Profile image should display centered with rounded corners
  </how-to-verify>
  <verify>
    <automated>jekyll build && echo "Build OK"</automated>
  </verify>
  <acceptance_criteria>
    - Jekyll build succeeds (exit code 0)
    - Human has visually verified the site at 375px and 1440px
    - Human has confirmed new fonts are rendering (not JetBrains Mono / Manrope)
    - Human has confirmed header feels redesigned
    - Human has confirmed footer has no gray backgrounds on mobile
    - Human has confirmed buttons have readable hover states
    - Human has confirmed book promo layout works at both viewports
    - Human has confirmed /blog/ returns 404
  </acceptance_criteria>
  <done>Human has verified the complete visual redesign at desktop and mobile viewports and approved the result.</done>
  <resume-signal>Type "approved" if the design is satisfactory, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Purchase links -> external sites | User clicks link to Amazon or Bitcoin Bazar |
| Markdown content -> HTML rendering | Jekyll processes Markdown with embedded HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Amazon/Bitcoin Bazar purchase links | mitigate | `target="_blank" rel="noopener"` on all external purchase links prevents reverse tabnabbing |
| T-01-02 | Tampering | Inline HTML in Markdown | accept | Only site author edits Markdown files. No user-generated content. Jekyll sanitization sufficient. |
| T-01-03 | Information Disclosure | Cover image metadata | accept | Book cover is a marketing asset -- public by design. No sensitive EXIF data concern. |
</threat_model>

<verification>
After Task 1 (before checkpoint):
1. `jekyll build && echo "Build OK"` -- site builds
2. `grep "B0GV1N6S1W" _site/dictionnaire/index.html` -- Amazon broche in build output
3. `grep "B0GV1X52YD" _site/dictionnaire/index.html` -- Amazon relie in build output
4. `grep "bitcoinbazar.fr" _site/dictionnaire/index.html` -- Bitcoin Bazar in build output
5. `test ! -d _site/blog && test ! -d _site/post && echo "BLOG GONE"` -- no blog in output
6. `grep -c "style=" _site/index.html` -- should be minimal (only from layout, not from content)

After checkpoint approval: phase is complete.
</verification>

<success_criteria>
- Book promo section renders correctly on dictionary page with all purchase links functional
- No blog files exist in repository or build output
- No inline styles in content pages
- Jekyll build succeeds
- Human approves visual design at both mobile (375px) and desktop (1440px) viewports
- The site feels "modern, sober, professional, and distinctive" per user's vision (D-01, D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-foundation-book-launch-page/01-02-SUMMARY.md`
</output>
