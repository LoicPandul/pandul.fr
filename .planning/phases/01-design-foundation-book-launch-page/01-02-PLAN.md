---
phase: 01-design-foundation-book-launch-page
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - dictionnaire.md
  - index.md
  - apropos.md
autonomous: false
requirements:
  - BOOK-01
  - BOOK-02
  - BOOK-03

must_haves:
  truths:
    - "Dictionary page displays book cover image prominently above the fold"
    - "Dictionary page has two Amazon purchase links (broche and relie) with correct ASINs"
    - "Dictionary page has a Bitcoin Bazar purchase link"
    - "No inline styles remain in content pages (index.md, apropos.md)"
  artifacts:
    - path: "dictionnaire.md"
      provides: "Book promo section with cover image and purchase links"
      contains: "book-promo"
    - path: "index.md"
      provides: "Homepage without inline styles"
    - path: "apropos.md"
      provides: "About page without inline styles"
  key_links:
    - from: "dictionnaire.md"
      to: "assets/img/cover-dictionnaire.png"
      via: "img src with relative_url filter"
      pattern: "cover-dictionnaire.png.*relative_url"
    - from: "dictionnaire.md"
      to: "https://www.amazon.fr/dp/B0GV1N6S1W"
      via: "anchor href"
      pattern: "amazon.fr/dp/B0GV1N6S1W"
    - from: "dictionnaire.md"
      to: "https://www.amazon.fr/dp/B0GV1X52YD"
      via: "anchor href"
      pattern: "amazon.fr/dp/B0GV1X52YD"
    - from: "dictionnaire.md"
      to: "https://bitcoinbazar.fr/"
      via: "anchor href"
      pattern: "bitcoinbazar.fr"
---

<objective>
Create the book promotion section on the dictionary landing page and clean up inline styles from content pages.

Purpose: Deliver the time-critical book launch promotion (book releasing in ~1 week) and ensure all content pages use the new design system CSS rather than inline styles.
Output: Rewritten dictionnaire.md with book promo, cleaned index.md and apropos.md.
</objective>

<execution_context>
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-foundation-book-launch-page/01-RESEARCH.md
@.planning/phases/01-design-foundation-book-launch-page/01-UI-SPEC.md
@.planning/phases/01-design-foundation-book-launch-page/01-VALIDATION.md
@.planning/phases/01-design-foundation-book-launch-page/01-01-SUMMARY.md
</context>

<interfaces>
<!-- CSS classes available from Plan 01 (defined in style.css) -->
<!-- Executor should use these directly -- Plan 01 must be completed first. -->

From assets/css/style.css (created by Plan 01):

.book-promo { margin: var(--space-lg) 0 var(--space-xl); }
.book-promo-inner { display: flex; gap: var(--space-xl); align-items: flex-start; }
.book-cover img { max-width: 280px; width: 100%; border-radius: var(--radius); border: 1px solid rgba(0,0,0,0.12); }
.book-info h2 { inherits JetBrains Mono H2 styles, margin-top: 0; }
.book-links { display: flex; flex-wrap: wrap; gap: var(--space-sm); margin-top: var(--space-md); }
.btn-primary { orange accent CTA button }
.btn-secondary { outlined secondary button }

Mobile responsive: .book-promo-inner stacks vertically and centers on less than 720px.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite dictionnaire.md with book promo section (BOOK-01, BOOK-02, BOOK-03)</name>
  <files>dictionnaire.md</files>
  <read_first>
    - dictionnaire.md (current content -- old description text, inline-styled cover image)
    - .planning/phases/01-design-foundation-book-launch-page/01-UI-SPEC.md (copywriting contract, layout patterns, Amazon URLs, button classes)
    - .planning/phases/01-design-foundation-book-launch-page/01-RESEARCH.md (book promo code example)
    - assets/css/style.css (verify book-promo CSS classes exist from Plan 01)
  </read_first>
  <action>
Rewrite `dictionnaire.md` completely. The new file must have:

**Front matter** -- keep the existing YAML front matter with title "Dictionnaire" and permalink /dictionnaire/.

**Page heading** -- immediately after front matter:

# Dictionnaire de Bitcoin

## Tout le vocabulaire technique de Bitcoin.

**Book promo section** -- immediately after the subtitle, insert this exact HTML block (per UI-SPEC copywriting contract and layout pattern):

The section element has class="book-promo". Inside it, a div with class="book-promo-inner" contains two children:

1. A div with class="book-cover" containing an img tag:
   - src="{{ '/assets/img/cover-dictionnaire.png' | relative_url }}"
   - alt="Couverture -- Dictionnaire de Bitcoin"
   - loading="eager"

2. A div with class="book-info" containing:
   - An h2: "Dictionnaire de Bitcoin -- Le Livre"
   - A p: "1408 definitions du vocabulaire technique de Bitcoin. Disponible en version imprimee."
   - A div with class="book-links" containing three anchor tags:

   Link 1 (Amazon Broche):
   - href="https://www.amazon.fr/dp/B0GV1N6S1W"
   - class="btn btn-primary"
   - target="_blank" rel="noopener"
   - Text: "Amazon -- Broche"

   Link 2 (Amazon Relie):
   - href="https://www.amazon.fr/dp/B0GV1X52YD"
   - class="btn btn-primary"
   - target="_blank" rel="noopener"
   - Text: "Amazon -- Relie"

   Link 3 (Bitcoin Bazar):
   - href="https://bitcoinbazar.fr/"
   - class="btn btn-secondary"
   - target="_blank" rel="noopener"
   - Text: "Acheter sur Bitcoin Bazar"

Use proper French accented characters in the rendered text (e.g. "definitions" with accent, "Broche" with accent, "Relie" with accent, "imprimee" with accent).

**After the book promo section**, add an hr (horizontal rule) followed by a brief description:

"Cet ouvrage recense tous les termes techniques en rapport avec Bitcoin ou son environnement, ainsi que les definitions correspondantes."

"Redige par Loic Morel, de Pandul, il est accessible gratuitement en version numerique sur ce depot GitHub." (with "ce depot GitHub" linked to https://github.com/LoicPandul/Dictionnaire-de-Bitcoin)

"Dictionnaire de Bitcoin est un projet ouvert. Vos contributions et relectures sont les bienvenues sur GitHub." (with "GitHub" linked to https://github.com/LoicPandul/Dictionnaire-de-Bitcoin)

Use proper French accented characters throughout.

**Important:** Do NOT include any inline style="" attributes or style blocks. All styling comes from CSS classes defined in Plan 01.

**Important:** The old text mentioning "846 definitions" and "PDF download" and "contribution instructions with numbered steps" should be removed -- the book is now published with 1408 definitions. Keep the essence (open project, contributions welcome) but make it concise.

**Note:** The Bitcoin Bazar URL (https://bitcoinbazar.fr/) is a placeholder per STATE.md -- it may need updating before launch. Use it as-is for now.
  </action>
  <verify>
    <automated>jekyll build && grep "cover-dictionnaire" _site/dictionnaire/index.html && grep "B0GV1N6S1W" _site/dictionnaire/index.html && grep "B0GV1X52YD" _site/dictionnaire/index.html && grep "bitcoinbazar.fr" _site/dictionnaire/index.html && grep "book-promo" _site/dictionnaire/index.html && echo "BOOK PROMO PASS"</automated>
  </verify>
  <acceptance_criteria>
    - dictionnaire.md contains class="book-promo"
    - dictionnaire.md contains class="book-promo-inner"
    - dictionnaire.md contains class="book-cover"
    - dictionnaire.md contains class="book-info"
    - dictionnaire.md contains class="book-links"
    - dictionnaire.md contains https://www.amazon.fr/dp/B0GV1N6S1W (Amazon broche)
    - dictionnaire.md contains https://www.amazon.fr/dp/B0GV1X52YD (Amazon relie)
    - dictionnaire.md contains https://bitcoinbazar.fr/ (Bitcoin Bazar)
    - dictionnaire.md contains btn btn-primary (2 occurrences for Amazon links)
    - dictionnaire.md contains btn btn-secondary (1 occurrence for Bitcoin Bazar)
    - dictionnaire.md contains target="_blank" rel="noopener" on all purchase links
    - dictionnaire.md contains loading="eager" on cover image
    - dictionnaire.md contains cover-dictionnaire.png referenced via relative_url
    - dictionnaire.md does NOT contain style=" (no inline styles)
    - dictionnaire.md does NOT contain 846 (old definition count removed)
    - jekyll build succeeds and _site/dictionnaire/index.html exists
  </acceptance_criteria>
  <done>Dictionary page displays the book promotion section with cover image (eager loading), two Amazon purchase links (broche B0GV1N6S1W and relie B0GV1X52YD), and a Bitcoin Bazar link. Content is concise and up-to-date (1408 definitions). No inline styles.</done>
</task>

<task type="auto">
  <name>Task 2: Clean inline styles from index.md and apropos.md</name>
  <files>index.md, apropos.md</files>
  <read_first>
    - index.md (current content -- contains inline styles and style blocks for course thumbnails)
    - apropos.md (current content -- contains inline style on avatar image)
    - assets/css/style.css (verify available CSS classes for avatar styling)
  </read_first>
  <action>
**apropos.md changes:**

The avatar image currently uses inline styles in a wrapping p tag:
- p has style="text-align:center"
- img has style="border-radius: 12px;"

Replace with CSS class usage. Add these rules to assets/css/style.css (via Edit, append after the utilities section):
- .avatar { border-radius: 12px; }
- .avatar--center { display: block; margin: 0 auto; }

Then in apropos.md, replace the avatar block. Remove the wrapping p tag with style="text-align:center". Use just the img tag with class="avatar avatar--center", keeping all other attributes (src with relative_url, alt, width="180", height="180", loading="lazy", decoding="async"). No style="" attribute.

Keep all other content in apropos.md unchanged -- the text, links, and front matter are fine as-is.

**index.md changes:**

Read index.md to identify all inline style="" attributes and style blocks. For each one:
- If it controls layout/spacing that can use existing CSS classes or new utility classes, replace with class.
- If it is a page-specific visual treatment (like course thumbnail hover effects), move the CSS rules to assets/css/style.css as named classes and apply those classes in the HTML.

Common patterns to look for in index.md:
- style blocks -- move rules to style.css with appropriate class names
- style="..." on images -- replace with CSS classes
- Any hardcoded colors -- must reference CSS variables

Add any new utility classes needed to style.css via Edit (append before the responsive sections).

**Important:** Do NOT change the content, links, or structure of index.md -- only remove inline styles and replace with CSS classes. The homepage content will be fully redesigned in Phase 4 (Portfolio Redesign). Phase 1 only cleans up inline styles.
  </action>
  <verify>
    <automated>grep -c 'style=' apropos.md; test $? -eq 1 && echo "apropos clean" || echo "apropos has inline styles"; grep -c 'avatar--center' apropos.md && echo "avatar class present"; jekyll build && echo "Build OK"</automated>
  </verify>
  <acceptance_criteria>
    - apropos.md does NOT contain style=" (no inline styles)
    - apropos.md contains class="avatar avatar--center" on the profile image
    - apropos.md does NOT contain p style="text-align:center"
    - assets/css/style.css contains .avatar class definition
    - assets/css/style.css contains .avatar--center class definition
    - index.md has no style blocks (moved to style.css)
    - index.md has fewer style= attributes than before (ideally zero)
    - jekyll build succeeds (exit code 0)
  </acceptance_criteria>
  <done>All inline styles removed from apropos.md (avatar uses CSS classes). Index.md inline styles moved to style.css with named classes. Jekyll build succeeds. All content pages now rely entirely on the design system CSS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Phase 1</name>
  <files>none (visual checkpoint)</files>
  <action>
Human visual verification of the complete Phase 1 deliverable. Run jekyll serve --livereload and verify the site in browser at multiple viewports.

What was built: Complete Phase 1 design foundation -- new black/white/orange design system, mobile-first responsive layout, book promotion section on dictionary page with Amazon and Bitcoin Bazar purchase links, cleaned navigation with x.com links, blog removed, inline styles cleaned from all content pages.

Steps to verify:
1. Run `jekyll serve --livereload` in terminal
2. Open http://localhost:4000 in browser
3. Desktop (1440px): Verify white background, black text, orange accent (#D04819) on hover. Header shows Accueil, Dictionnaire, A propos + X/GitHub icons.
4. Navigate to /dictionnaire/: Verify book cover image displays prominently with title "Dictionnaire de Bitcoin -- Le Livre", two orange Amazon buttons (Broche + Relie), and one outlined Bitcoin Bazar button. Cover image should be on the left, info on the right.
5. Click Amazon buttons: Verify they open amazon.fr in new tabs with correct ASINs (B0GV1N6S1W and B0GV1X52YD).
6. Mobile (375px via DevTools): Verify responsive layout -- header stacks, book promo stacks (image on top, centered), buttons stack. Text is readable.
7. Navigate to /a-propos/: Verify avatar image displays centered, no visual breaks.
8. Navigate to /blog/: Verify it returns 404 (blog deleted).
9. Check footer: X and GitHub icons present, links point to x.com.

Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>
    <automated>jekyll build && echo "Build OK"</automated>
  </verify>
  <done>Human has visually verified the complete Phase 1 deliverable at desktop and mobile viewports and approved the result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External purchase links | Amazon and Bitcoin Bazar links open in new tabs from dictionary page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Spoofing | External purchase links (Amazon, Bitcoin Bazar) | mitigate | All target="_blank" links include rel="noopener" explicitly in HTML |
| T-1-02 | Tampering | Book cover image path | accept | Static image served from same origin, no user-controlled paths |
</threat_model>

<verification>
After all tasks complete:
1. `jekyll build && echo "Build OK"` -- full site builds
2. `grep 'cover-dictionnaire' _site/dictionnaire/index.html` -- book cover in output
3. `grep 'B0GV1N6S1W' _site/dictionnaire/index.html` -- Amazon broche ASIN
4. `grep 'B0GV1X52YD' _site/dictionnaire/index.html` -- Amazon relie ASIN
5. `grep 'bitcoinbazar.fr' _site/dictionnaire/index.html` -- Bitcoin Bazar link
6. `test ! -d _site/blog && test ! -d _site/post && echo "No blog"` -- blog removed
7. `grep -c 'site-nav' _site/index.html` -- navigation renders
8. Human visual verification at 375px and 1440px widths
</verification>

<success_criteria>
- Dictionary page shows book promo section with cover image, 2 Amazon links, 1 Bitcoin Bazar link
- All purchase links open in new tabs with rel="noopener"
- No inline styles in dictionnaire.md, apropos.md
- Index.md inline styles moved to CSS classes
- Jekyll build succeeds
- Human approves visual appearance at mobile and desktop widths
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-foundation-book-launch-page/01-02-SUMMARY.md`
</output>
