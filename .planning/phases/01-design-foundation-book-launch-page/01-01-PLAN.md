---
phase: 01-design-foundation-book-launch-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/style.css
  - _layouts/default.html
  - _config.yml
  - blog.md
  - post/
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Site renders with black (#000) / white (#FFF) / orange (#D04819) palette"
    - "Navigation shows exactly: Accueil, Dictionnaire, A propos + X and GitHub icons"
    - "All pages are responsive and readable at 375px mobile width"
    - "Blog pages and post directory no longer exist"
    - "Typography uses Manrope body (line-height 1.7, max-width 680px) and JetBrains Mono headings"
  artifacts:
    - path: "assets/css/style.css"
      provides: "Complete design system with mobile-first responsive CSS"
      contains: "--accent: #D04819"
    - path: "_layouts/default.html"
      provides: "Updated layout with x.com links and aria-labels"
      contains: "x.com/Loic_Pandul"
    - path: "_config.yml"
      provides: "Config without post exclusion and with jekyll-feed removed"
  key_links:
    - from: "assets/css/style.css"
      to: "_layouts/default.html"
      via: "CSS classes consumed by layout HTML"
      pattern: "site-header|header-card|site-nav|site-footer|footer-card"
---

<objective>
Rewrite the design system (CSS) and update the site layout for the new black/white/orange palette, mobile-first responsive approach, modern typography, and blog removal.

Purpose: Establish the visual foundation that all Phase 1 pages depend on, and remove legacy blog content.
Output: Rewritten style.css, updated default.html, cleaned _config.yml, deleted blog files.
</objective>

<execution_context>
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-foundation-book-launch-page/01-RESEARCH.md
@.planning/phases/01-design-foundation-book-launch-page/01-UI-SPEC.md
@.planning/phases/01-design-foundation-book-launch-page/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite style.css with new design system (UI-01, UI-02, UI-05)</name>
  <files>assets/css/style.css</files>
  <read_first>
    - assets/css/style.css (current state — 244 lines, desktop-first, old palette)
    - .planning/phases/01-design-foundation-book-launch-page/01-UI-SPEC.md (design contract — THE source of truth for all values)
    - .planning/phases/01-design-foundation-book-launch-page/01-RESEARCH.md (code examples section)
    - _layouts/default.html (to understand all CSS classes consumed by the layout)
  </read_first>
  <action>
Rewrite `assets/css/style.css` completely. The new file must implement the full UI-SPEC design contract. Here are the exact specifications:

**CSS Custom Properties (`:root`)** — per UI-SPEC "CSS Custom Properties" section:
```css
:root {
  --bg: #FFFFFF;
  --fg: #000000;
  --brand: #000000;
  --accent: #D04819;
  --accent-hover: #B83D14;
  --muted: #666666;
  --line: #E5E5E5;
  --wrap: 1100px;
  --prose: 680px;
  --radius: 10px;
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
  --space-2xl: 48px;
  --space-3xl: 64px;
}
```

**Font faces** — keep existing @font-face declarations for JetBrains Mono and Manrope (same `truetype-variations` format, same `font-display: swap`).

**Reset** — keep `* { box-sizing: border-box }`, update `html,body` to use `--bg` and `--fg` variables, `line-height: 1.65`, Manrope font stack.

**Typography** — per UI-SPEC "Typography" section:
- Body: Manrope, `clamp(16px, 1.05vw + 14px, 18px)`, weight 400, line-height 1.7
- H1: JetBrains Mono, `clamp(20px, 4vw, 28px)`, weight 700, line-height 1.2, letter-spacing -0.02em
- H2: JetBrains Mono, `clamp(16px, 3vw, 22px)`, weight 700, line-height 1.2, letter-spacing -0.005em
- H3: JetBrains Mono, `clamp(14px, 2.5vw, 18px)`, weight 700, line-height 1.3, letter-spacing -0.005em
- Only 2 weights: 400 (regular) and 700 (bold). NO 450 or 600.
- `.site-main p, li, blockquote, dd, dt` get `max-width: var(--prose)` for reading comfort.
- `code, kbd, samp, pre` use JetBrains Mono, weight 400 (not 450).

**Links** — per UI-SPEC "Interaction States":
- Default: `color: var(--fg)`, `text-underline-offset: 2px`
- Hover/focus: `color: var(--accent)`
- `a.icon`: `text-decoration: none; line-height: 0`

**Header** — keep sticky behavior (position: sticky, top: 16px, z-index: 1000):
- `.header-card`: background `var(--brand)`, color `#fff`, border-radius `var(--radius)`, padding `var(--space-md) 20px`, flex row, `align-items: center`, `justify-content: space-between`, same box-shadow and transition.
- `.header-card.hide`: same transform translateY(-140%).
- `.brand`: flex, `align-items: center`, `gap: 14px`, `text-decoration: none`, `color: #fff`.
- `.brand img`: `height: 32px`.
- `.brand-name`: `color: #fff`, `font-size: 18px`, `letter-spacing: .3px`.
- `.site-nav a`: `margin-left: 18px`, `text-decoration: none`, `color: #fff`, `opacity: .95`, `padding: var(--space-xs) 2px`, `border-radius: 6px`, transition on color and opacity.
- `.site-nav a:hover, a:focus`: `color: var(--accent)`, `opacity: 1`.
- `.site-nav a.active`: `color: var(--accent)`.
- `.site-nav .icon img`: `height: 20px`, `vertical-align: middle`, `filter: invert(1)`, `opacity: .95`.

**Footer** — per UI-SPEC color section:
- `.site-footer`: `margin-top: var(--space-2xl)`.
- `.footer-card`: `background: #000`, `color: #fff`, `border-radius: var(--radius)`, `padding: var(--space-xl)`, CSS Grid `grid-template-columns: 1.4fr 1fr 1.2fr`, `gap: 12px`, `align-items: center`.
- `.footer-brand img`: `height: 56px`.
- `.footer-nav a`: `display: block`, `color: #fff`, `text-decoration: none`, `margin: 6px 0`.
- `.footer-nav a:hover`: `color: var(--accent)`.
- `.footer-bottom .divider`: `height: 1px`, `background: var(--line)` (was #eaeaea, now use token).
- `.copyright`: `color: var(--muted)`.

**Buttons** — per UI-SPEC "Button Sizing" table (for Plan 02's book promo, but define CSS now):
- `.btn-primary`: `display: inline-block`, `background: var(--accent)`, `color: #FFFFFF`, `border: none`, `padding: 12px 24px`, `border-radius: var(--radius)`, `font-weight: 700`, `font-size: 16px`, `text-decoration: none`, `min-height: 44px`, `transition: background 0.15s ease`.
- `.btn-primary:hover`: `background: var(--accent-hover)`.
- `.btn-primary:focus`: `outline: 2px solid var(--accent); outline-offset: 2px`.
- `.btn-secondary`: `display: inline-block`, `background: transparent`, `color: var(--fg)`, `border: 1px solid var(--line)`, `padding: 12px 24px`, `border-radius: var(--radius)`, `font-weight: 400`, `font-size: 16px`, `text-decoration: none`, `min-height: 44px`, `transition: background 0.15s ease, color 0.15s ease`.
- `.btn-secondary:hover`: `background: var(--fg); color: #FFFFFF`.
- `.btn-secondary:focus`: `outline: 2px solid var(--fg); outline-offset: 2px`.

**Book promo section** — define CSS for Plan 02 to consume:
- `.book-promo`: `margin: var(--space-lg) 0 var(--space-xl)`.
- `.book-promo-inner`: `display: flex`, `gap: var(--space-xl)`, `align-items: flex-start`.
- `.book-cover img`: `max-width: 280px`, `width: 100%`, `height: auto`, `border-radius: var(--radius)`, `border: 1px solid rgba(0,0,0,0.12)`.
- `.book-info h2`: inherit JetBrains Mono H2 styles, `margin-top: 0`.
- `.book-links`: `display: flex`, `flex-wrap: wrap`, `gap: var(--space-sm)`, `margin-top: var(--space-md)`.

**Responsive** — MOBILE-FIRST (base = 375px, enhance upward with `min-width`):

Base mobile styles (no media query):
- `.header-card`: `flex-direction: column`, `align-items: center`, `gap: var(--space-sm)`, `padding: var(--space-sm) 12px`, `border-radius: 12px`.
- `.site-header`: `top: 8px`.
- `.brand img`: `height: 22px`.
- `.brand-name`: `font-size: 15px`, `letter-spacing: 0`.
- `.site-nav`: `display: flex`, `flex-wrap: wrap`, `justify-content: center`, `gap: 8px 10px`, `width: 100%`.
- `.site-nav a`: `margin: 0`, `padding: 5px 7px`, `font-size: 14px`, `border-radius: 8px`, `background: rgba(255,255,255,.06)`.
- `.site-nav .icon`: `padding: 5px`.
- `.site-nav .icon img`: `height: 16px`.
- `.site-main .wrap`: `padding: 14px 20px 24px`.
- `.footer-card`: `grid-template-columns: 1fr`, `text-align: center`, `justify-items: center`, `padding: 22px`, `border-radius: 14px`.
- `.footer-brand`: `flex-direction: column`, `gap: 8px`.
- `.footer-brand img`: `height: 44px`.
- `.footer-nav`: `display: flex`, `flex-wrap: wrap`, `justify-content: center`, `gap: 10px 12px`.
- `.footer-nav a`: `margin: 0`, `padding: 8px 10px`, `border-radius: 10px`, `background: #0f0f0f`.
- `.book-promo-inner`: `flex-direction: column`, `align-items: center`, `text-align: center`.
- `.book-cover img`: `max-width: 320px`.

`@media (min-width: 720px)`:
- `.site-header`: `top: 16px`.
- `.header-card`: `flex-direction: row`, `padding: var(--space-md) 20px`, `border-radius: var(--radius)`.
- `.brand img`: `height: 32px`.
- `.brand-name`: `font-size: 18px`, `letter-spacing: .3px`.
- `.site-nav`: `flex-wrap: nowrap`, `gap: 0`, `width: auto`.
- `.site-nav a`: `margin-left: 18px`, `padding: var(--space-xs) 2px`, `font-size: inherit`, `border-radius: 6px`, `background: transparent`.
- `.site-nav .icon`: `padding: 0`.
- `.site-nav .icon img`: `height: 20px`.
- `.site-main .wrap`: `padding: 12px 24px 24px`.
- `.footer-card`: `grid-template-columns: 1fr 1fr`, `text-align: left`, `justify-items: start`, `padding: var(--space-xl)`, `border-radius: var(--radius)`.
- `.footer-brand`: `flex-direction: row`, `gap: 15px`.
- `.footer-brand img`: `height: 56px`.
- `.footer-nav`: `display: block`.
- `.footer-nav a`: `padding: 0`, `border-radius: 0`, `background: transparent`.
- `.book-promo-inner`: `flex-direction: row`, `align-items: flex-start`, `text-align: left`.
- `.book-cover img`: `max-width: 280px`.

`@media (min-width: 980px)`:
- `.footer-card`: `grid-template-columns: 1.4fr 1fr 1.2fr`.

`@media (max-width: 380px)` (keep for very small screens):
- `.brand-name`: `font-size: 14px`.
- `.site-nav a`: `font-size: 13px`, `padding: 4px 6px`.

**Separators**: `hr { border: 0; border-top: 1px solid var(--line); margin: 28px 0 }`.

**Utilities**: `.tiny { font-size: .9rem; color: var(--muted); text-align: center; margin: var(--space-md) 0 var(--space-xl) }`.

**Images**: `img { max-width: 100%; height: auto }`.

**Important:** Do NOT use any hardcoded color values in the stylesheet except inside `:root` and in specific exceptions (nav mobile backgrounds `rgba(255,255,255,.06)`, footer mobile nav background `#0f0f0f`, book cover border `rgba(0,0,0,0.12)`, header card shadow `rgba(0,0,0,.18)`). All other colors must reference CSS variables.

Comments in French per project convention.
  </action>
  <verify>
    <automated>grep -- "--accent: #D04819" assets/css/style.css && grep -- "--prose: 680px" assets/css/style.css && grep "min-width: 720px" assets/css/style.css && grep "min-width: 980px" assets/css/style.css && grep "btn-primary" assets/css/style.css && grep "book-promo" assets/css/style.css && echo "CSS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - assets/css/style.css contains `--accent: #D04819`
    - assets/css/style.css contains `--accent-hover: #B83D14`
    - assets/css/style.css contains `--bg: #FFFFFF`
    - assets/css/style.css contains `--fg: #000000`
    - assets/css/style.css contains `--prose: 680px`
    - assets/css/style.css contains `--space-xs: 4px`
    - assets/css/style.css contains `--space-3xl: 64px`
    - assets/css/style.css contains `min-width: 720px` (mobile-first)
    - assets/css/style.css contains `min-width: 980px`
    - assets/css/style.css does NOT contain `max-width: 720px` (old desktop-first)
    - assets/css/style.css does NOT contain `max-width: 640px` (old breakpoint)
    - assets/css/style.css does NOT contain `max-width: 980px` (old breakpoint)
    - assets/css/style.css contains `.btn-primary`
    - assets/css/style.css contains `.btn-secondary`
    - assets/css/style.css contains `.book-promo`
    - assets/css/style.css contains `.book-promo-inner`
    - assets/css/style.css contains `.book-cover`
    - assets/css/style.css contains `.book-links`
    - assets/css/style.css does NOT contain `#FF5C00` (old accent)
    - assets/css/style.css does NOT contain `font-weight: 450` (removed weight)
    - H1 line contains `font-weight: 700` (not 450)
    - H2 line contains `font-weight: 700` (not 450)
  </acceptance_criteria>
  <done>style.css implements the full UI-SPEC design contract: new color palette (#D04819 accent), mobile-first responsive (min-width breakpoints at 720px and 980px), modern typography (2-weight system 400/700, clamp sizes, line-height 1.7, prose max-width 680px), spacing scale, button styles, and book promo section CSS. No old palette colors or desktop-first breakpoints remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update layout, delete blog, clean config (UI-03, UI-04)</name>
  <files>_layouts/default.html, _config.yml, blog.md, post/</files>
  <read_first>
    - _layouts/default.html (current layout — check twitter.com links, aria-labels)
    - _config.yml (check plugins list, exclude list)
    - blog.md (confirm exists before deletion)
    - .planning/phases/01-design-foundation-book-launch-page/01-UI-SPEC.md (icon accessibility section)
    - .planning/phases/01-design-foundation-book-launch-page/01-RESEARCH.md (blog deletion pitfalls, twitter->x.com)
  </read_first>
  <action>
**_layouts/default.html changes:**

1. Update X (Twitter) links from `twitter.com` to `x.com` in both header and footer. Change ALL occurrences of `https://twitter.com/Loic_Pandul` to `https://x.com/Loic_Pandul`. There are 2 occurrences (line 26 header, line 64 footer).

2. Update `aria-label` on X icon links from `aria-label="X"` to `aria-label="X (anciennement Twitter)"` per UI-SPEC. There are 2 occurrences (header line 26, footer line 64).

3. Verify GitHub icon links already have `aria-label="GitHub"` — they do (lines 29, 68). No change needed.

4. Navigation structure is already correct (Accueil, Dictionnaire, A propos + X/GitHub icons). No structural changes needed for UI-03.

**Blog deletion (UI-04):**

1. Delete `blog.md` file.
2. Delete `post/` directory and all its contents (6 subdirectories: `bip47-le-vilain-petit-canard`, `bitcoin-stamps-utxo-set-et-fonctionnement-d-utreexo`, `combien-faut-il-faire-de-remix-sur-whirlpool`, `comprendre-et-utiliser-le-coinjoin-sur-bitcoin`, `contribuer-sur-le-dictionnaire-de-bitcoin`, `maitriser-les-bases-de-l-analyse-de-chaine-sur-bitcoin`).

**_config.yml changes:**

1. Remove `jekyll-feed` from the `plugins` list (blog is being deleted, feed plugin generates RSS for blog posts which no longer exist). Keep `jekyll-seo-tag`, `jekyll-sitemap`, `jekyll-redirect-from`.
2. Remove `post/` from the `exclude` list (directory is being deleted, no need to exclude it). The `exclude` key can be removed entirely since `post/` was its only entry.
3. Keep everything else unchanged: title, description, author, lang, timezone, url, baseurl, permalink, include, logo, social, defaults.
  </action>
  <verify>
    <automated>grep "x.com/Loic_Pandul" _layouts/default.html && grep "anciennement Twitter" _layouts/default.html && test ! -f blog.md && test ! -d post && grep -v "jekyll-feed" _config.yml > /dev/null && grep -v "post/" _config.yml > /dev/null && echo "LAYOUT+BLOG PASS"</automated>
  </verify>
  <acceptance_criteria>
    - _layouts/default.html contains `https://x.com/Loic_Pandul` (NOT twitter.com)
    - _layouts/default.html does NOT contain `twitter.com`
    - _layouts/default.html contains `aria-label="X (anciennement Twitter)"` (2 occurrences)
    - _layouts/default.html contains `aria-label="GitHub"` (2 occurrences)
    - _layouts/default.html contains nav links: Accueil, Dictionnaire, A propos
    - blog.md file does NOT exist
    - post/ directory does NOT exist
    - _config.yml does NOT contain `jekyll-feed`
    - _config.yml does NOT contain `post/` in exclude
    - _config.yml still contains `jekyll-seo-tag`
    - _config.yml still contains `jekyll-sitemap`
    - _config.yml still contains `jekyll-redirect-from`
    - `jekyll build` succeeds (exit code 0)
  </acceptance_criteria>
  <done>Layout updated with x.com URLs and improved aria-labels. Blog fully removed (blog.md deleted, post/ directory deleted, jekyll-feed plugin removed from config, post/ removed from exclude list). Jekyll build succeeds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External links | Purchase links to Amazon and Bitcoin Bazar open in new tabs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Spoofing | External links in layout (X, GitHub) | mitigate | All `target="_blank"` links include `rel="noopener"` — already present in default.html |
| T-1-02 | Information Disclosure | Deleted blog content | accept | Blog posts were already excluded from build; physical deletion removes them from repo entirely |
</threat_model>

<verification>
After both tasks complete:
1. `jekyll build && echo "Build OK"` — site builds without errors
2. `grep -c 'site-nav' _site/index.html` — navigation renders
3. `test ! -d _site/blog && test ! -d _site/post && echo "No blog"` — blog fully gone
4. `grep '#D04819' assets/css/style.css` — new accent color in place
5. `grep 'min-width: 720px' assets/css/style.css` — mobile-first confirmed
</verification>

<success_criteria>
- Site builds successfully with `jekyll build`
- New black/white/orange design system active in style.css
- Mobile-first responsive CSS (min-width breakpoints, no max-width except 380px edge case)
- Typography uses 2-weight system (400/700) with proper clamp sizes
- Navigation links and social icons updated to x.com with proper aria-labels
- Blog completely removed from filesystem and config
- Button and book-promo CSS classes defined and ready for Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-foundation-book-launch-page/01-01-SUMMARY.md`
</output>
