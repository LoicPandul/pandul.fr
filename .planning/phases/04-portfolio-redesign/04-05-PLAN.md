---
phase: 04-portfolio-redesign
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [index.md, assets/css/style.css]
autonomous: true
gap_closure: true
requirements: [PORT-01, PORT-02, PORT-03, PORT-04, PORT-05]

must_haves:
  truths:
    - "Hero section is modern and polished with quote marker and integrated content navigation"
    - "Dictionary section matches dictionnaire.md design with title, paragraph, and 3 buttons"
    - "Platform badges display company logo images instead of text"
    - "Section headers have no emoji icons"
    - "All changes are responsive on mobile (375px)"
  artifacts:
    - path: "index.md"
      provides: "Redesigned hero with quote + nav, dict section with buttons, logo badges, no emojis"
      min_lines: 100
    - path: "assets/css/style.css"
      provides: "Updated hero CSS, dict section CSS, platform-badge-logo CSS, responsive mobile"
      min_lines: 1400
  key_links:
    - from: "index.md"
      to: "assets/css/style.css"
      via: "CSS class names"
      pattern: "hero-section|dict-promo|platform-badge-logo"
    - from: "index.md"
      to: "/dictionnaire/"
      via: "Explorer le dictionnaire button href"
      pattern: "dictionnaire"
---

<objective>
Redesign the homepage hero section, dictionary section, and platform badges based on UAT feedback. Remove emoji icons. Ensure all changes are mobile-responsive.

Purpose: User reported hero is ugly, dict section doesn't match the dictionary page design, badges should be logos not text, and emojis should be removed. This plan addresses all visual/structural gaps in index.md and style.css.
Output: Polished, modern homepage with cohesive hero+nav area, dictionary section matching dictionnaire.md style, logo-based platform badges, and full mobile responsiveness.
</objective>

<execution_context>
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-portfolio-redesign/04-02-SUMMARY.md

<interfaces>
<!-- Existing design system variables from style.css :root -->
--bg: #FFFFFF
--fg: #000000
--brand: #000000
--accent: #D04819
--accent-hover: #B83D14
--muted: #666666
--line: #E5E5E5
--wrap: 1100px
--prose: 680px
--radius: 6px
--surface: #F5F5F5
--font-heading: "Outfit"
--font-body: "Inter"
--space-xs through --space-3xl (4px to 64px)

<!-- Existing button classes -->
.btn-primary (orange filled, white text, outline on hover)
.btn-secondary (black filled, white text, outline on hover)
.btn-sm (padding: 8px 16px, font-size: 14px, min-height: 36px)

<!-- Dictionary page design reference (dictionnaire.md) -->
dict-hero: two-column layout with h1 + stats on left, intro paragraph on right
definition-cta: bordered card with rows, each row has heading + buttons
definition-cta-buttons: flex wrap with btn btn-primary btn-sm and btn btn-secondary btn-sm

<!-- Available logo images (all 1500x600px with built-in colored backgrounds) -->
assets/img/pbn.png (PBN - 104 tutoriels + 6 formations)
assets/img/bitstack.png (Bitstack - 81 articles)
assets/img/decouvre-bitcoin.png (Decouvre Bitcoin - 6 videos + interview Theo Pantamis)
assets/img/cryptoast.png (Space Cryptoast entry)
assets/img/gab.png (CoinJoins entry)
assets/img/htb.png (Confidentialite/anonymat entry)

<!-- Platform-to-logo mapping for Liquid template -->
PBN -> pbn.png
Bitstack -> bitstack.png
Decouvre Bitcoin -> decouvre-bitcoin.png
Cryptoast -> cryptoast.png
GAB -> gab.png
HTB -> htb.png
Apple Podcasts, Spotify, X -> keep as text badges (no logo available)

<!-- Responsive breakpoints -->
980px (tablet landscape)
719px (mobile)
380px (small mobile)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign hero, dictionary section, badges, and remove emojis in index.md</name>
  <files>index.md</files>
  <action>
    Rewrite the index.md template with these changes. Add proper French accents to ALL French text in this file.

    **1. Hero section redesign — merge hero + section-nav into one cohesive area:**

    Replace the current separate hero-section and section-nav with a unified hero area. The design concept:
    - Profile photo on the left (or top on mobile)
    - Quote-styled title: add a ">" marker before the main phrase. Render this as a visual blockquote-style element (use a CSS class like `hero-quote` with a left border accent or a large typographic ">" character). The text is: "J'ecris des contenus pedagogiques sur Bitcoin." (with proper accent on "pedagogiques" -> "pedagogiques" and "ecris" -> "ecris")
    - Below the quote, integrate the 4 content navigation links (Dictionnaire, Formations, Ecrits, Audio / Video) as styled pills/buttons within the hero area itself, not as a separate nav bar
    - The navigation links still use #anchor hrefs for smooth scroll

    Suggested HTML structure:
    ```html
    <section class="hero-section">
      <div class="hero-content">
        <img src="{{ '/assets/img/profile.png' | relative_url }}" alt="Loic Morel" class="hero-photo" loading="eager">
        <div class="hero-text">
          <h1 class="hero-quote"><span class="hero-quote-mark">></span> J'ecris des contenus pedagogiques sur Bitcoin.</h1>
          <nav class="hero-nav" aria-label="Sections de contenu">
            <a href="#dictionnaire">Dictionnaire</a>
            <a href="#formations">Formations</a>
            <a href="#ecrits">Ecrits</a>
            <a href="#audio-video">Audio / Video</a>
          </nav>
        </div>
      </div>
    </section>
    ```
    (Use proper accents: "ecris" -> "ecris" with accent, "pedagogiques" -> "pedagogiques" with accents. Adjust structure as needed for best design outcome.)

    Remove the old standalone `<nav class="section-nav">` block entirely — it's now inside the hero.

    **2. Dictionary section redesign — match dictionnaire.md style:**

    Replace the current dict-promo-card with a design that mirrors the dictionary landing page. Use the same CTA pattern from dictionnaire.md (definition-cta). Structure:

    ```html
    <section id="dictionnaire" class="portfolio-section">
      <div class="dict-homepage-hero">
        <h2>Dictionnaire de Bitcoin</h2>
        <p class="dict-homepage-stats">1408 definitions dans 19 categories</p>
        <p class="dict-homepage-desc">Le Dictionnaire de Bitcoin est un ouvrage open source qui recense l'ensemble des definitions couvrant le vocabulaire technique de Bitcoin et de son ecosysteme. Il est disponible gratuitement en ligne ou bien a la vente en version imprimee.</p>
      </div>
      <div class="dict-homepage-cta">
        <div class="dict-homepage-buttons">
          <a href="https://www.amazon.fr/dp/B0GV1N6S1W" class="btn-primary btn-sm" target="_blank" rel="noopener">Acheter sur Amazon</a>
          <a href="https://bitcoinbazar.fr/" class="btn-primary btn-sm" target="_blank" rel="noopener">Acheter sur Bitcoin Bazar</a>
          <a href="{{ '/dictionnaire/' | relative_url }}" class="btn-secondary btn-sm">Explorer le dictionnaire</a>
        </div>
      </div>
    </section>
    ```
    (Apply proper French accents to all text: "definitions" -> "definitions" with accent, "categories" -> "categories" with accent, "ecosysteme" -> "ecosysteme" with accents, "imprimee" -> "imprimee" with accent, etc.)

    **3. Remove emoji icons from section headers:**

    In the Formations, Ecrits, and Audio / Video sections, remove the `<span class="section-header-icon" aria-hidden="true">...</span>` elements entirely. Keep the section-header-left div but without the icon span. Add accents to "Ecrits" -> "Ecrits" with accent in the h2.

    **4. Platform badges — replace text with logo images:**

    Change the platform-badge rendering in ALL sections (featured cards + compact lists) to use images when a logo is available:

    For the featured card sections and compact list sections, replace:
    ```html
    <span class="platform-badge platform-badge--{{ item.platform | downcase }}">{{ item.platform }}</span>
    ```

    With a conditional that checks if a logo exists for the platform:
    ```html
    {% assign platform_lower = item.platform | downcase %}
    {% if platform_lower == "pbn" or platform_lower == "bitstack" or platform_lower == "decouvre bitcoin" or platform_lower == "cryptoast" or platform_lower == "gab" or platform_lower == "htb" %}
      <img class="platform-badge-logo" src="{{ '/assets/img/' | append: platform_lower | replace: ' ', '-' | append: '.png' | relative_url }}" alt="{{ item.platform }}" loading="lazy">
    {% else %}
      <span class="platform-badge platform-badge--{{ platform_lower }}">{{ item.platform }}</span>
    {% endif %}
    ```

    This pattern must be applied in 6 places in index.md:
    - Formations featured cards (1 badge)
    - Formations compact list (1 badge)
    - Ecrits featured cards (1 badge)
    - Ecrits compact list (1 badge)
    - Audio/Video featured cards (1 badge)
    - Audio/Video compact list (1 badge)

    To avoid repeating this logic 6 times, consider extracting it into a reusable Liquid include or just repeat it (Jekyll doesn't have great include-with-variables support for inline use). Repeating is fine for 6 instances.

    **5. French accents in index.md text:**
    Fix all French text: "Toutes les formations" -> with accents, "Tous les ecrits" -> "Tous les ecrits" with accent, "contenus audio et video" -> "contenus audio et video" with accents, etc.
  </action>
  <verify>
    <automated>grep -c "section-header-icon" index.md | grep "^0$" && grep "hero-quote" index.md && grep "dict-homepage-hero" index.md && grep "platform-badge-logo" index.md && echo "All HTML changes applied"</automated>
  </verify>
  <done>index.md has: unified hero with quote style and integrated nav, dictionary section with 3 buttons matching dictionnaire.md design, no emoji icon spans, logo-based platform badges with fallback to text badges, proper French accents throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Update CSS for hero, dict section, logo badges, and mobile responsive</name>
  <files>assets/css/style.css</files>
  <action>
    Update the Phase 4 CSS section in style.css. Replace the existing hero, section-nav, dict-promo-card, and platform-badge styles. Keep all non-Phase-4 CSS untouched.

    **1. Hero section CSS — polished, modern design:**

    Apply ui-ux-pro-max mindset: this is a portfolio hero, it should feel refined and intentional.

    ```css
    .hero-section {
      padding: var(--space-3xl) 0 var(--space-2xl);
      max-width: var(--wrap);
      margin: 0 auto;
    }

    .hero-content {
      display: flex;
      align-items: flex-start;
      gap: var(--space-xl);
    }

    .hero-photo {
      width: 100px;
      height: 100px;
      border-radius: var(--radius);
      flex-shrink: 0;
      display: block;
    }

    .hero-text {
      flex: 1;
    }

    .hero-quote {
      font-family: var(--font-heading);
      font-size: clamp(22px, 3.5vw, 28px);
      font-weight: 700;
      line-height: 1.3;
      letter-spacing: -0.02em;
      color: var(--fg);
      margin: 0 0 var(--space-lg);
      position: relative;
    }

    .hero-quote-mark {
      color: var(--accent);
      font-family: var(--font-mono);
      font-weight: 700;
      margin-right: var(--space-xs);
      user-select: none;
    }
    ```

    The hero-nav (content navigation links integrated into hero):
    ```css
    .hero-nav {
      display: flex;
      gap: var(--space-sm);
      flex-wrap: wrap;
    }

    .hero-nav a {
      display: inline-flex;
      align-items: center;
      padding: 8px 16px;
      border-radius: var(--radius);
      text-decoration: none;
      font-family: var(--font-heading);
      font-size: 14px;
      font-weight: 600;
      color: var(--muted);
      background: var(--surface);
      border: 1px solid var(--line);
      transition: all 0.15s ease;
    }

    .hero-nav a:hover {
      color: var(--fg);
      border-color: var(--accent);
      background: var(--bg);
    }
    ```

    Remove the old `.section-nav` styles entirely (the standalone nav bar CSS).

    **2. Dictionary homepage section CSS:**

    Style the new dict-homepage-hero and dict-homepage-cta to visually match the dictionary landing page's aesthetic (clean, centered, with prominent buttons):

    ```css
    .dict-homepage-hero {
      text-align: center;
      margin-bottom: var(--space-lg);
    }

    .dict-homepage-hero h2 {
      font-family: var(--font-heading);
      font-weight: 700;
      font-size: clamp(22px, 3.5vw, 26px);
      color: var(--fg);
      margin: 0 0 var(--space-xs);
    }

    .dict-homepage-stats {
      font-family: var(--font-body);
      font-size: 15px;
      color: var(--muted);
      margin: 0 0 var(--space-md);
    }

    .dict-homepage-desc {
      font-family: var(--font-body);
      font-size: 15px;
      line-height: 1.7;
      color: var(--muted);
      max-width: 640px;
      margin: 0 auto;
    }

    .dict-homepage-cta {
      text-align: center;
      padding: var(--space-lg) 0;
    }

    .dict-homepage-buttons {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: var(--space-sm);
    }
    ```

    Remove the old `.dict-promo-card`, `.dict-promo-stats`, `.dict-promo-desc`, `.dict-promo-content` styles entirely.

    **3. Platform badge logo CSS:**

    Add styles for the new logo-based badges. Logos are 1500x600px (5:2 ratio) with built-in colored backgrounds — just round the corners:

    ```css
    .platform-badge-logo {
      height: 20px;
      width: auto;
      border-radius: 4px;
      display: inline-block;
      vertical-align: middle;
      flex-shrink: 0;
    }
    ```

    In the content-list-item context, the logo should align properly:
    ```css
    .content-list-item .platform-badge-logo {
      height: 18px;
    }

    .content-card-body .platform-badge-logo {
      height: 22px;
      align-self: flex-start;
    }
    ```

    **4. Remove section-header-icon CSS:**

    Remove the `.section-header-icon` rule since emoji icons are gone. Keep `.section-header`, `.section-header-left`, `.section-header h2`, `.section-count`.

    **5. Mobile responsive updates:**

    Update the existing Phase 4 responsive breakpoints:

    At 980px:
    ```css
    .hero-photo { width: 88px; height: 88px; }
    ```

    At 719px (mobile):
    ```css
    .hero-content {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }
    .hero-photo { width: 80px; height: 80px; }
    .hero-quote { font-size: clamp(20px, 5vw, 24px); }
    .hero-nav { justify-content: center; }
    .dict-homepage-buttons { flex-direction: column; align-items: center; }
    .dict-homepage-buttons .btn-primary,
    .dict-homepage-buttons .btn-secondary { width: 100%; max-width: 280px; }
    .platform-badge-logo { height: 16px; }
    .content-list-item .platform-badge-logo { height: 14px; }
    ```

    At 380px:
    ```css
    .hero-photo { width: 72px; height: 72px; }
    .hero-nav a { font-size: 13px; padding: 6px 12px; }
    ```

    **6. Update prefers-reduced-motion:**
    Add `.hero-nav a` to the list of elements with `transition: none`.

    **IMPORTANT:** When editing style.css, locate the Phase 4 section (starting around line 1113 with the comment "Homepage -- Portfolio redesign (Phase 4)") and replace the hero, section-nav, dict-promo, section-header-icon, and platform-badge styles. Keep all responsive breakpoints updated. Do NOT modify any CSS before the Phase 4 section (header, footer, dictionary, buttons, etc.).
  </action>
  <verify>
    <automated>grep "hero-quote" assets/css/style.css && grep "dict-homepage" assets/css/style.css && grep "platform-badge-logo" assets/css/style.css && grep -c "section-nav " assets/css/style.css | grep "^0$" && echo "All CSS changes applied"</automated>
  </verify>
  <done>style.css has: modern hero with quote mark and integrated nav, dictionary homepage section matching landing page aesthetic, logo badge sizing with rounded corners, emoji icon CSS removed, full mobile responsive at 980px/719px/380px breakpoints, reduced-motion updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External links | Amazon and Bitcoin Bazar purchase links open in new tabs with rel="noopener" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | S (Spoofing) | External purchase links | mitigate | rel="noopener" on all target="_blank" links prevents reverse tabnapping |
| T-04-03 | I (Information Disclosure) | Logo images | accept | Public company logos, no sensitive data |
</threat_model>

<verification>
1. `jekyll build` completes without errors
2. Hero section renders with quote marker, photo, and integrated navigation links
3. Dictionary section has 3 buttons (Amazon, Bitcoin Bazar, Explorer le dictionnaire)
4. No emoji unicode characters in section headers
5. Logo images render for PBN, Bitstack, Decouvre Bitcoin, Cryptoast, GAB, HTB platforms
6. Text badges still render for Apple Podcasts, Spotify, X platforms
7. Mobile layout (375px) shows single-column hero, stacked buttons, smaller logos
</verification>

<success_criteria>
- Hero is visually polished with quote styling and integrated content navigation
- Dictionary section matches dictionnaire.md design with title + paragraph + 3 buttons
- Platform badges show logo images for 6 platforms with rounded corners
- No emoji icons in section headers
- All responsive breakpoints work (980px, 719px, 380px)
- Jekyll build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-portfolio-redesign/04-05-SUMMARY.md`
</output>
