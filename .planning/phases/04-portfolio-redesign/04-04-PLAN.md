---
phase: 04-portfolio-redesign
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [_data/formations.yml, _data/ecrits.yml, _data/audio-video.yml]
autonomous: true
gap_closure: true
requirements: [PORT-01, PORT-04]

must_haves:
  truths:
    - "All French text in YAML data files has proper accents (e, e, e, a, u, etc.)"
    - "audio-video.yml has correct platform values for logo mapping"
    - "Three deprecated entries are removed from audio-video.yml"
  artifacts:
    - path: "_data/formations.yml"
      provides: "French text with proper accents for all 6 entries"
      contains: "pedagogiques"
    - path: "_data/ecrits.yml"
      provides: "French text with proper accents for all ~185 entries"
      contains: "securite"
    - path: "_data/audio-video.yml"
      provides: "French text with proper accents, corrected platforms, 3 entries deleted"
      contains: "confidentialite"
  key_links:
    - from: "_data/formations.yml"
      to: "index.md"
      via: "site.data.formations Liquid loop"
      pattern: "site\\.data\\.formations"
    - from: "_data/audio-video.yml"
      to: "index.md"
      via: "site.data.audio-video Liquid loop"
      pattern: "site\\.data\\.audio-video"
---

<objective>
Fix all French accent issues across the 3 YAML data files and apply audio-video.yml data corrections (platform reassignments and entry deletions).

Purpose: User reported accents missing everywhere in French text. Additionally, 4 entries in audio-video.yml need platform corrections for logo mapping, and 3 entries must be deleted entirely.
Output: Clean, properly-accented YAML data files ready for homepage rendering with correct platform associations.
</objective>

<execution_context>
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/loicm/Documents/GitHub/pandul.fr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-portfolio-redesign/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add French accents to all 3 YAML data files</name>
  <files>_data/formations.yml, _data/ecrits.yml, _data/audio-video.yml</files>
  <action>
    Read each YAML file and systematically fix ALL missing French accents in title, description, and any other text fields. Common accent fixes needed:

    **Systematic replacements (apply contextually, not blindly):**
    - "e" that should be "e" (accent aigu): pedagogiques, securite, confidentialite, generalise, ameliorer, reseaux, derivation, cle, cles, decentralise, chiffrement, numerique, integrite, authenticite, proteger, preserver, securise, specifique, fongible, reutilisables, regulierement, detenteurs, realite, vulnerabilite, methodes, compromis, explorateurs, prouver, etc.
    - "e" that should be "e" (accent grave): systeme, modele, probleme, premiere, derniere, critere, etc.
    - "e" that should be "e" (accent circonflexe): etre, meme, etc.
    - "a" that should be "a" (accent grave): a (preposition "a"), deja, la (article in some contexts), etc.
    - "u" that should be "u" (accent grave): ou (where), sur (some contexts)

    **IMPORTANT rules:**
    - Do NOT change URLs — accents only in human-readable text (title, description fields)
    - Do NOT change platform field values — those are machine identifiers
    - Do NOT change type field values
    - Do NOT change field names (title, url, description, platform, type, featured)
    - Preserve YAML structure exactly (indentation, dashes, quotes)
    - Technical terms in English stay as-is (Bitcoin, Lightning, Taproot, SegWit, RGB, etc.)
    - French technical terms get accents: "reseau" -> "reseau" with accent on first e, "cle" -> "cle" with accent
    - Comments in YAML should also get accents fixed

    **For formations.yml (6 entries):** Fix accents in title and description fields. Example: "Comprendre, installer, configurer et utiliser un noeud Bitcoin" -> "Comprendre, installer, configurer et utiliser un noeud Bitcoin" (noeud stays, but "Ameliorer" -> "Ameliorer" with accent, "securite" -> "securite" with accents, etc.)

    **For ecrits.yml (~185 entries):** Same accent fixes across all entries. This is the largest file.

    **For audio-video.yml (~57 entries before deletions):** Same accent fixes plus the data corrections in Task 2.

    Be thorough — the user specifically called out that accents are missing EVERYWHERE. Every single French word that needs an accent must get one.
  </action>
  <verify>
    <automated>grep -c "securite\b" _data/*.yml; grep -c "securite\b" _data/*.yml; echo "If securite count is 0 and securite count > 0, accents are applied"</automated>
  </verify>
  <done>All French text in formations.yml, ecrits.yml, and audio-video.yml has proper accents. No accent-less French words remain in human-readable text fields.</done>
</task>

<task type="auto">
  <name>Task 2: Fix audio-video.yml platform values and delete 3 entries</name>
  <files>_data/audio-video.yml</files>
  <action>
    Apply these specific data corrections to audio-video.yml:

    **Platform reassignments (change platform field value):**
    1. Entry with title containing "Comment preserver sa confidentialite et son anonymat avec Bitcoin" (currently platform: "YouTube") -> change to platform: "HTB"
    2. Entry with title "Space Cryptoast : UTXOs et confidentialite sur Bitcoin" (currently platform: "X") -> change to platform: "Cryptoast"
    3. Entry with title "Proteger vos crypto avec les CoinJoins" (currently platform: "YouTube") -> change to platform: "GAB"
    4. Entry with title containing "Interview technique avec le docteur en mathematiques appliquees Theo Pantamis" (currently platform: "YouTube") -> change to platform: "Decouvre Bitcoin"

    **Delete these 3 entries entirely (remove the full YAML block for each):**
    1. Title containing "Self-custody pour tous les detenteurs" (or with accents after Task 1)
    2. Title containing "Notre expert en securite Bitcoin" (or with accents)
    3. Title containing "Wasabi vs Samourai" (or with accents)

    After deletions, audio-video.yml should have 54 entries (was 57, minus 3).

    NOTE: Task 1 accent fixes and Task 2 data fixes should be applied together in the same file write. Read the file, apply accents everywhere, then apply platform changes, then delete the 3 entries, then write.
  </action>
  <verify>
    <automated>grep -c "^- title:" _data/audio-video.yml | grep "54" && grep "platform: \"HTB\"" _data/audio-video.yml && grep "platform: \"Cryptoast\"" _data/audio-video.yml && grep "platform: \"GAB\"" _data/audio-video.yml && echo "All platform fixes and deletions applied"</automated>
  </verify>
  <done>audio-video.yml has exactly 54 entries. "Comment preserver..." has platform HTB, "Space Cryptoast..." has platform Cryptoast, "Proteger vos crypto..." has platform GAB, "Interview technique... Theo Pantamis" has platform "Decouvre Bitcoin". The 3 deprecated entries (Self-custody, Notre expert, Wasabi vs Samourai) are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — static data files only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | YAML data | accept | Static files, no user input, version controlled in git |
</threat_model>

<verification>
1. `jekyll build` completes without errors
2. All 3 YAML files parse correctly (no YAML syntax errors)
3. grep confirms accent patterns present (securite, confidentialite, pedagogiques)
4. audio-video.yml entry count is 54
5. New platform values (HTB, Cryptoast, GAB) are present
</verification>

<success_criteria>
- All French text across 248+ data entries has proper accents
- audio-video.yml reflects 4 platform reassignments and 3 deletions
- YAML syntax is valid (Jekyll build succeeds)
</success_criteria>

<output>
After completion, create `.planning/phases/04-portfolio-redesign/04-04-SUMMARY.md`
</output>
